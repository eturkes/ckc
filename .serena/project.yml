# Serena LSP config, tracked (CLAUDE.md). Omitted keys take serena defaults;
# full key/language reference: https://github.com/oraios/serena
project_name: "ckc"
# first entry = fallback LS; after adding a language, ask the user to /mcp-reconnect serena
languages:
- rust
- bash
encoding: "utf-8"
ignore_all_files_in_gitignore: true
