wip-report-m2.1 (M2.23 salvage) — redo procedure + .1b resolved design
=======================================================================
The .1 session overflowed its 200K window post-derivation: metrics.rs half GREEN,
report.rs partial written. Recovery pre-proved every .1a gate on the banked tree,
compile-verified the .1b partial, banked both as source-diffs, reverted. Redo reads
THIS file + targeted in-file anchors only — SPEC re-reads unneeded (§ facts inlined).
Delete: 1a.diff at .1a close; 1b.diff + this file at .1b close.

.1a — metrics.rs canonical layer (PROVEN GREEN)
-----------------------------------------------
Redo = `git apply .agent/wip-report-m2.1a.diff` → gates → close. Transcribe NOTHING.
Gates all PRE-PROVEN on the banked tree; expect exactly:
- `cargo test -p ckc-cli` → 231 passed, 7 ignored (`--lib metrics::` alone = 22)
- `cargo fmt -p ckc-cli --check` clean; `cargo clippy -p ckc-cli --all-targets -- -D warnings` clean
- `RUSTDOCFLAGS='-D warnings' cargo doc -p ckc-cli --no-deps` → 18 errors (standing, no new)
- engine/quant audit on metrics.rs clean (reconstruct per memory engine-agnostic bullet)
Diff content (orientation; design docs are in-code): Canonical+CanonRead for
MetricRow {metric, value?} — NotApplicable = OMITTED `value` member (present row sans
value = §7.3 not_applicable; metric absent from rows = no row + omission diagnostic —
distinct states; read = obj.optional → map_or(NotApplicable, Value)); RouteMetrics
{diagnostics: emit_set, pipeline_id, rows: emit_set} (rows sort by leading `metric`
member bytes == metric-id order; ids unique); RouteDelta {pipeline_id, rows: emit_set};
ExperimentMetrics {baseline_pipeline_id, raw_rows: emit_array(self.routes),
route_deltas: emit_array(self.deltas)} — KEYS LOAD-BEARING: §4.3 sorts member keys, so
`raw_rows` < `route_deltas` carries §9 raw-before-delta into the canonical BYTES
(memory REPORT-m2.1 TRAP discharged); Rust fields stay routes/deltas. Plus
emission_order + module docs (contract tense). Tests appended after
violations_beyond_calls_are_a_fill_bug: metric_row_bytes_omit_value_on_not_applicable
pins {"metric":"acceptance_rate","value":{"den":"2","num":"1"}} and
{"metric":"k_sample_convergence"}; experiment_metrics_round_trip_with_raw_rows_before_deltas
pins find("\"raw_rows\":") < find("\"route_deltas\":") + strict round-trip, via
delta_fixture() + experiment_metrics(vec![route, baseline], &static_id("pipe.base")).
Commit: `report (M2.23): metrics.rs canonical layer — §9 raw_rows<route_deltas bytes (.1a)`

.1b — report.rs M2 shape + populated pin (needs .1a)
----------------------------------------------------
Apply `.agent/wip-report-m2.1b.diff` (COMPILE-VERIFIED: cargo check 0 errors; its 2
unused-import warnings vanish once the remainder lands). It carries: module-doc M2
sentence; imports ModelIdentity/emit_array/read_array (ckc-core re-exports ✓) +
`use crate::metrics::ExperimentMetrics;`; RouteTaxonomy {categories: Vec<(Id,u64)>,
codes: Vec<(Id,u64)>, pipeline_id: Id} + Canonical (emit_u64_map ×2 + pipeline_id) +
CanonRead (read_u64_map ×2), inserted before Report; Report-doc omit-None sentence.

Remainder — every decision RESOLVED. ✓ = re-verified during recovery; V = verify
in-file at redo (report.rs is the edit target — zero extra read). report.rs = 2023
lines: touch targeted regions only (Report struct/emit/read, validate, helpers
check_canonical_set/check_map_order, tests from `fn id` / valid_report on); metrics.rs
only through the applied .1a diff + `pub fn experiment_metrics` grep.

1. Report + 3 Option fields (alphabetical slots): failure_taxonomy:
   Option<Vec<RouteTaxonomy>>, metrics: Option<ExperimentMetrics>, model_identity:
   Option<ModelIdentity>. Canonical emit: obj.optional("failure_taxonomy", …,
   emit_array) after diagnostics_summary; obj.optional("metrics") after lexicon_hash;
   obj.optional("model_identity") after metrics. CanonRead positional, full ascending
   order ✓: corpus_hashes < diagnostics_summary < failure_taxonomy < findings <
   lexicon_hash < metrics < model_identity < no_conflict_results < replay_status <
   solver_identity < wording. Copy the .1a ExperimentMetrics emit/read idioms.
   Omit-None ⇒ the existing PINNED_REPORT literal stays UNTOUCHED (M1 regression
   guard — never edit it).
2. assemble_report's Report literal + valid_report() + render_markdown_marks_empty_slots
   literal: each gains the 3 None fields (M1 behavior; M2.24 populates).
3. validate() extension (+ its doc list). failure_taxonomy Some(v): v non-empty else
   EmptySection{pool:"failure_taxonomy"}; pipeline_ids unique else DuplicateRoute{pool:
   "failure_taxonomy", pipeline_id}; per route check_map_order on categories AND codes
   (V signatures in-file) + each count > 0 else ZeroCount (generalize its Display
   "diagnostics_summary count…" → "summary count for {code} is zero"; existing tests
   match variants not Display ✓). metrics Some(m): m.routes non-empty else
   EmptySection{pool:"raw_rows"}; route pipeline_ids unique else DuplicateRoute{pool:
   "raw_rows",…}; per route check_map_order("rows", metric ids) + check_canonical_set
   ("diagnostics"); m.baseline_pipeline_id ∈ routes else BaselineMissing(Id); delta
   pipeline_ids == non-baseline routes IN routes order else DeltaTables; per delta
   check_map_order("rows"). Taxonomy↔metrics route-set cross-check: deliberately NOT
   here (assembly consistency = M2.24's job). New ReportError variants + Display arms:
   EmptySection{pool:&'static str}, DuplicateRoute{pool:&'static str, pipeline_id:Id},
   BaselineMissing(Id), DeltaTables.
4. populated_report() fixture beside valid_report() (id() helper ✓):
   valid_report() base, then —
   - findings[0] quoted span texts → 投与を推奨する / 投与しないこと (V field names
     in-file; emit_string passes raw UTF-8 → byte-pin safe; write literals raw, never \u).
   - wording = [DocumentedNoConflictResult, LockedMeasurement,
     SyntheticTestSourceMeasurement] (V exact variant names in-file).
   - failure_taxonomy = Some(vec![direct, single_ir]) — pipe.m2_direct_smt: categories
     [(semantic_contradiction,1),(semantic_no_conflict,2),(target_syntax_failure,1)],
     codes [(ai_schema_violation,2),(repair_limit_exceeded,1),(target_parse_error,1)];
     pipe.m2_single_ir: categories [(semantic_contradiction,1),(semantic_no_conflict,3)],
     codes [(ai_hallucinated_source,1),(ai_schema_violation,1)]. §6 category + §7.4 code
     snake tokens as ids; every list above pre-sorted by id bytes ✓.
   - metrics = Some(crate::metrics::experiment_metrics(vec![direct_rm, single_ir_rm],
     &id("pipe.m2_direct_smt"))) — pub fn(Vec<RouteMetrics>, &Id) ✓, preserves input
     route order ✓, asserts per-route rows sorted+unique ✓ (hand-built rows below are
     pre-sorted ✓); the real assembler computes the delta table. direct_rm rows:
     acceptance_rate 1/2, conflict_verdict_accuracy 1/2, k_sample_convergence NA,
     recorded_call_count 4/1, repair_count 1/1, schema_valid_rate 3/4,
     target_syntactic_validity 2/3; diagnostics []. single_ir_rm rows: acceptance_rate
     1/1, k_sample_convergence NA, recorded_call_count 3/1, repair_count 0/1,
     schema_valid_rate 2/3, target_syntactic_validity 1/1 — NO conflict_verdict_accuracy
     row; diagnostics [omitted_metric_diagnostic("conflict_verdict_accuracy", <reason>)]
     (metrics.rs fn — make pub(crate); mirror its existing metrics-test call). Expected
     assembler deltas (single_ir − baseline; sanity vs the pin): acceptance_rate 1/2,
     conflict_verdict_accuracy NA, k_sample_convergence NA, recorded_call_count -1/1,
     repair_count -1/1, schema_valid_rate -1/12, target_syntactic_validity 1/3.
   - model_identity = Some(ModelIdentity{model_id: id("model.baseline"), quant:
     "fixture_quant".into(), runtime_version: "1.0.0".into()}) — synthetic tokens
     (engine-agnostic rule).
5. Tests: (a) populated_report strict round-trip + PINNED_POPULATED_REPORT byte pin —
   PIN FROM OBSERVED output (placeholder → run → paste actual; raw UTF-8 JA); (b)
   all-None emitted bytes contain NONE of the keys "failure_taxonomy"/"metrics"/
   "model_identity" (existing PINNED_REPORT test untouched beside it); (c) populated
   bytes: find("\"raw_rows\":") < find("\"route_deltas\":") — §9 order in report.json
   bytes; (d) §0 wording pin: populated bytes contain "wording":["documented
   no-conflict result","locked measurement","synthetic test source measurement"] (V
   exact wire labels from the Wording enum/PINNED_REPORT); (e) validate rejections, one
   per new variant: empty taxonomy vec, dup taxonomy route, zero count, empty
   m.routes, dup metrics route, missing baseline, wrong delta set/order (DeltaTables) +
   MapOrder via unsorted categories AND unsorted delta rows (V MapOrder pool naming
   from check_map_order).
6. Gates: cargo test (workspace); cargo fmt --check; cargo clippy --workspace
   --all-targets -- -D warnings; doc-lint BOTH crates (standing 18 ckc-cli / 17
   ckc-core, no new); engine/quant audit on report.rs + metrics.rs; §0 vocabulary via
   test (d).
Close: rm .agent/wip-report-m2.1b.diff + this file; context %; stub; commit
`report (M2.23): report.json M2 shape + populated canonical pin (.1b)`
