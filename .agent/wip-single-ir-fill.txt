# wip — route-single-ir.2b (single_ir_fill + 3 golden cassettes + reproduce-M1 gate)
# STATUS: PRE-VALIDATED end-to-end (M2.17 prep), then source reverted + banked.
# The exact validated run.rs change is in `.agent/wip-single-ir-fill.patch`.
# Redo = apply + bless + gate + commit. ZERO re-derivation; the gate self-checks correctness.

## What .2b delivers (all of it is in the patch)
`single_ir_fill(root, entry, lexicon, store, seed, resolved, repair_limit, shell)
  -> Option<ArtifactWrapper<IrBundle>>` in `crates/ckc-cli/src/run.rs` = the route's fill half:
extract → segment → build region+segment id-universes → `model_fill(store, key, Replay,
repair_limit, single_ir_accept(&regions, &segment_ids))` → deterministic tail (mirrors
`assemble_bundle`, segments-only diagnostics) → `IrBundle`. The patch also carries the .2b-only
imports, `single_ir_accept`'s `#[allow(dead_code)]`, a `write_single_ir_cassette` test helper
(reused by .4), an `#[ignore]`d `bless_single_ir_cassettes`, and the
`single_ir_fill_reproduces_m1_bundles` gate test.

## Banked validated state (proven THIS session before revert)
- `git apply .agent/wip-single-ir-fill.patch` re-applies CLEAN to HEAD (verified after revert).
- After apply + bless, `cargo test --workspace` GREEN; the gate test
  `single_ir_fill_reproduces_m1_bundles` PASSES — each per-doc `IrBundle.content_hash` EQUALS the M1
  `assemble_bundle` bundle (payload-only hash ⇒ producer-independent; self-checking, so a wrong tail
  fails LOUDLY) AND `route.payload == m1.payload` structurally. (Re-confirm exact pass counts at redo;
  the self-checking gate, not the count, is the guarantee.)
- fmt + clippy (`-D warnings`) clean; engine-agnostic audit on the new cassettes clean.

## Procedure (run in order)
1. `git apply .agent/wip-single-ir-fill.patch`
2. Bless the 3 golden cassettes (writes route.single_ir/<source>/seed-42.json):
   `cargo test -p ckc-cli bless_single_ir_cassettes -- --ignored`
3. Gate (the reproduce-M1 test runs here):  `cargo test --workspace`
4. Engine-agnostic audit — the new cassettes carry SYNTHETIC identity so they ARE audited (only
   live-recorded route.fixture/ is exempt). Cassettes are still untracked here → use the filesystem
   `rg` form (git grep sees only tracked files). Proven clean this session, expect exit 1:
   `/usr/bin/rg --no-ignore -niP '\b(llama|vllm|ollama|exllama|exllamav2|mlc|ctranslate2?|tensorrt|sglang|lmdeploy|gpt4all|kobold(cpp)?|ggml|gguf|safetensors|awq|gptq|exl2|gbnf|lark|qwen|mistral|gemma|deepseek|vicuna|bf16|fp16|fp8|int4|int8|nf4|q[2-8]_[0-9km])\b' crates/ckc-cli/tests/fixtures/cassettes/route.single_ir/`
   (Canonical full-repo form + rationale: memory "Engine-agnostic DELIVERABLE".)
5. `cargo fmt --all --check`  +  `cargo clippy --workspace --all-targets -- -D warnings`

## Close-out
- `git add` run.rs + the 3 cassettes; `rm .agent/wip-single-ir-fill.txt .agent/wip-single-ir-fill.patch`
  IN THE SAME COMMIT (both consumed).
- Record .2b context-usage (`.agent/context.sh`) into the roadmap .2b line; mark .2b DONE
  (M2 stays IN-PROGRESS — .3/.4 + run-m2.* remain).
- Commit `route (M2.17): single_ir_fill + 3 golden cassettes + reproduce-M1 gate`.

## If the patch fails to apply (only if run.rs changed since M2.17 prep)
The hunks show every exact edit — re-derive against current run.rs by hand; the gate test still
proves correctness. Design + signatures: roadmap .2b line + memory ("Model-fill stage core",
"M2 plan" single_ir bullets).
