diff --git a/crates/ckc-cli/src/lib.rs b/crates/ckc-cli/src/lib.rs
index 6578647..587c353 100644
--- a/crates/ckc-cli/src/lib.rs
+++ b/crates/ckc-cli/src/lib.rs
@@ -98,13 +98,17 @@
 //!   command's stdout body (rerun outcome and layout, every accepted
 //!   artifact hash), every failure one command-scope §7.4 diagnostic.
 //! - [`model`] — §9 recorded-subprocess model-runtime adapter
-//!   (`model-adapter.1`): [`model::ModelAdapter`] resolves the
+//!   (`model-adapter.1`/`.2`): [`model::ModelAdapter`] resolves the
 //!   environment-supplied runtime command on PATH (neutral default,
 //!   `CKC_MODEL_COMMAND`-overridable), probes its self-reported
-//!   [`ckc_core::ModelIdentity`], and runs one budgeted subprocess per call
+//!   [`ckc_core::ModelIdentity`], and runs budgeted subprocesses
 //!   ([`model::ModelRun`]/[`model::ModelOutcome`]: the prompt on stdin under
 //!   the committed constraint-path + seed CLI contract, generated bytes on
-//!   stdout); the forthcoming §7.4 model-fill stage drives it.
+//!   stdout) — one per [`model::ModelAdapter::invoke`], or a reproducible
+//!   k-sample draw under per-sample seeds via
+//!   [`model::ModelAdapter::invoke_samples`]; a clean exit is
+//!   [`model::ModelOutcome::Completed`] only once stdout reached EOF, else
+//!   capture-incomplete. The forthcoming §7.4 model-fill stage drives it.
 #![forbid(unsafe_code)]
 
 pub mod extract;
diff --git a/crates/ckc-cli/src/model.rs b/crates/ckc-cli/src/model.rs
index 2b88163..934781c 100644
--- a/crates/ckc-cli/src/model.rs
+++ b/crates/ckc-cli/src/model.rs
@@ -12,17 +12,22 @@
 //! contract is only the CLI shape — the probe flag, the generation args,
 //! and the stdin/stdout byte streams.
 //!
-//! This is the invoke SKELETON (`model-adapter.1`): it resolves the runtime,
-//! probes the identity, and runs one budgeted call feeding the prompt on
-//! stdin under the constraint-path + seed args, capturing the outcome. Real
-//! constrained decoding (the route's grammar/JSON-Schema) and k-sampling
-//! land in `model-adapter.2`; diagnostic mapping of a process failure is the
-//! §7.4 model-fill stage's job, so the adapter returns raw outcome data.
+//! `model-adapter.1` landed the resolve/probe/single-invoke skeleton;
+//! `model-adapter.2` completes it: [`ModelAdapter::invoke_samples`] draws k
+//! recorded samples under per-sample seeds (the §9 k-sample convergence
+//! mechanism), and a clean exit now mints [`ModelOutcome::Completed`] only
+//! once stdout reached EOF within the drain grace — a descendant holding the
+//! pipe past it surfaces as [`ModelOutcome::CaptureIncomplete`] rather than
+//! a silently truncated capture, since the generated bytes are recorded
+//! byte-for-byte and their byte-stability is the cassette's determinism.
+//! Diagnostic mapping of a process failure is the §7.4 model-fill stage's
+//! job, so the adapter still returns raw outcome data.
 
 use std::ffi::OsStr;
 use std::io::{Read, Write};
 use std::path::{Path, PathBuf};
 use std::process::{Child, Command, ExitStatus, Stdio};
+use std::sync::atomic::{AtomicBool, Ordering};
 use std::sync::{Arc, Mutex};
 use std::thread::{self, JoinHandle};
 use std::time::{Duration, Instant};
@@ -111,6 +116,12 @@ impl ModelAdapter {
         );
         match &probe.outcome {
             ModelOutcome::Completed { .. } => {}
+            ModelOutcome::CaptureIncomplete { .. } => {
+                return Err(ModelAdapterError::Probe {
+                    detail: "identity probe stdout never reached EOF within the drain grace"
+                        .to_owned(),
+                });
+            }
             ModelOutcome::Timeout => {
                 return Err(ModelAdapterError::Probe {
                     detail: "identity probe hit its wall-clock budget".to_owned(),
@@ -153,10 +164,11 @@ impl ModelAdapter {
     /// back on stdout. The subprocess is killed on expiry; every fate —
     /// completed bytes, timeout, exit failure, spawn failure — comes back as
     /// data in the [`ModelRun`], with whatever stdout/stderr drained before
-    /// the end. This skeleton passes the constraint path verbatim — argv
-    /// carries it as `OsStr`, lossless even for a non-UTF-8 path — so
-    /// compiling it to the runtime's constraint format is the wrapper's job
-    /// (and the live constrained decoding wiring is `model-adapter.2`).
+    /// the end. The constraint path is passed verbatim — argv carries it as
+    /// `OsStr`, lossless even for a non-UTF-8 path — so compiling it to the
+    /// runtime's constraint format is the wrapper's job. This is the
+    /// single-sample primitive; [`invoke_samples`](Self::invoke_samples)
+    /// layers the §9 k-sample draw over it.
     pub fn invoke(&self, prompt: &str, constraint: &Path, seed: u64, budget: Duration) -> ModelRun {
         let seed = seed.to_string();
         let args = [
@@ -167,16 +179,47 @@ impl ModelAdapter {
         ];
         run_process(&self.program, &args, prompt.as_bytes(), budget)
     }
+
+    /// Draw `k` recorded samples for one prompt under one constraint (the §9
+    /// k-sample convergence mechanism). Sample `i` runs
+    /// [`invoke`](Self::invoke) under [`derive_seed`]`(base_seed, i)`, so the
+    /// whole draw is reproducible from `(base_seed, k)` and each sample
+    /// carries the seed the cassette keys it by. The recorded-call count is
+    /// the returned length (`k`; `k == 0` yields an empty draw). Whether the
+    /// samples diverge is the runtime's decoding mode — greedy yields
+    /// identical draws, sampling diverse ones — and out of the adapter's
+    /// engine-agnostic scope: it only supplies the per-sample seeds, and the
+    /// downstream stage measures convergence over the outputs.
+    pub fn invoke_samples(
+        &self,
+        prompt: &str,
+        constraint: &Path,
+        base_seed: u64,
+        k: u32,
+        budget: Duration,
+    ) -> Vec<ModelSample> {
+        (0..k)
+            .map(|i| {
+                let seed = derive_seed(base_seed, i);
+                ModelSample {
+                    seed,
+                    run: self.invoke(prompt, constraint, seed, budget),
+                }
+            })
+            .collect()
+    }
 }
 
 /// One model-runtime subprocess, raw. `stdout_bytes` holds the stdout
 /// drained within the run's budget plus the post-fate [`DRAIN_GRACE`] —
 /// kept as bytes (never lossy-decoded) because the generated output is
 /// recorded byte-for-byte for the cassette and its determinism is
-/// byte-stability. It is the complete output for a well-behaved runtime
-/// (one that closes stdout before exiting), but partial when a budget kill
-/// landed first or a descendant outlived the exit still holding the pipe
-/// open past the grace. `stderr` is the diagnostic stream, lossily decoded.
+/// byte-stability. It equals the full output exactly when the outcome is
+/// [`ModelOutcome::Completed`] (stdout reached EOF); a
+/// [`ModelOutcome::CaptureIncomplete`], `Timeout`, or `ExitFailure` leaves
+/// it a prefix — a kill landed first, or a descendant outlived the exit
+/// still holding the pipe open past the grace. `stderr` is the diagnostic
+/// stream, lossily decoded.
 #[derive(Debug, Clone, PartialEq, Eq)]
 pub struct ModelRun {
     pub outcome: ModelOutcome,
@@ -184,22 +227,39 @@ pub struct ModelRun {
     pub stderr: String,
 }
 
+/// One draw of a [`ModelAdapter::invoke_samples`] k-sample set: the
+/// [`derive_seed`]-derived seed and the run it produced. The cassette keys
+/// recorded calls by seed, so each sample carries its own rather than
+/// leaving the caller to re-derive it.
+#[derive(Debug, Clone, PartialEq, Eq)]
+pub struct ModelSample {
+    pub seed: u64,
+    pub run: ModelRun,
+}
+
 /// Process fate of one model invocation.
 #[derive(Debug, Clone, PartialEq, Eq)]
 pub enum ModelOutcome {
-    /// Exit status zero. `bytes` is the stdout drained before exit closed
-    /// the pipe: the full output for a well-behaved runtime (one that closes
-    /// stdout before exiting), but only the pre-[`DRAIN_GRACE`] prefix if a
-    /// descendant outlives the exit still holding the pipe open (see
-    /// [`ModelRun::stdout_bytes`]). Mirrors `Z3Adapter`'s success-only
+    /// Exit status zero AND stdout reached EOF within [`DRAIN_GRACE`], so the
+    /// capture is complete (every write end of the pipe closed, leaving
+    /// `bytes` the whole output). This is the only recordable success: a
+    /// clean exit whose stdout stayed open past the grace is
+    /// [`CaptureIncomplete`](ModelOutcome::CaptureIncomplete) instead, never
+    /// a silently truncated `Completed`. Mirrors `Z3Adapter`'s success-only
     /// payload — the adapter parses nothing (the §7.4 schema/grounding parse
     /// is the model-fill stage's job), so a completed run's "result" is
     /// simply its output bytes, also in [`ModelRun::stdout_bytes`].
-    /// Guaranteeing completeness against a pipe-holding descendant (a
-    /// process-group kill or an EOF-gated capture outcome) lands with the
-    /// cassette recording in `model-adapter.2`/`model-cassette`, where these
-    /// bytes become byte-stability load-bearing.
     Completed { bytes: Vec<u8> },
+    /// Exit status zero, but stdout never reached EOF within [`DRAIN_GRACE`]:
+    /// a descendant outlived the clean exit still holding the write end, so
+    /// the captured `bytes` are only a prefix of the true output. Kept
+    /// distinct from [`Completed`](ModelOutcome::Completed) — the generated
+    /// bytes are recorded byte-for-byte for the cassette, and recording a
+    /// truncated prefix as a success would silently corrupt that
+    /// byte-stability. The crate forbids `unsafe`, so the safe lever is to
+    /// surface incompleteness here (gate `Completed` on EOF) rather than
+    /// reap the pipe-holding process group via `libc`.
+    CaptureIncomplete { bytes: Vec<u8> },
     /// The wall-clock budget expired and the process was killed.
     Timeout,
     /// Nonzero exit status; `code` is `None` when a signal ended the
@@ -252,6 +312,20 @@ fn resolve_command(env_override: Option<String>) -> String {
     }
 }
 
+/// Per-sample seed for a k-sample draw: `seed_i = f(base_seed, i)`. A
+/// splitmix64 finalizer over `base_seed + i·φ` (the 64-bit golden-ratio odd
+/// constant). Deterministic and reproducible — the cassette re-derives the
+/// same seed for `(base_seed, i)` — while decorrelating sequential `i`, so a
+/// runtime seeding its PRNG from the value draws independent samples rather
+/// than near-duplicates off adjacent seeds. Pure (no env, no `unsafe`),
+/// hence directly unit-tested.
+fn derive_seed(base_seed: u64, i: u32) -> u64 {
+    let mut z = base_seed.wrapping_add((i as u64).wrapping_mul(0x9E37_79B9_7F4A_7C15));
+    z = (z ^ (z >> 30)).wrapping_mul(0xBF58_476D_1CE4_E5B9);
+    z = (z ^ (z >> 27)).wrapping_mul(0x94D0_49BB_1331_11EB);
+    z ^ (z >> 31)
+}
+
 /// Parse a [`ModelIdentity`] from a probe reply: one `key=value` per line,
 /// order-independent, extra lines ignored, first occurrence of each key
 /// kept. All three fields must be present and non-empty, and `model_id` must
@@ -295,20 +369,38 @@ fn require_field(value: Option<String>, name: &str) -> Result<String, String> {
     }
 }
 
-/// Reader thread draining one output pipe into a shared buffer until EOF
-/// (write ends close at exit or kill), so neither pipe can fill and stall
-/// the child. The buffer is shared so the runner can snapshot partial
-/// output without joining: a killed process's orphans inherit the write ends
-/// and would hold a join hostage.
-fn drain<R: Read + Send + 'static>(pipe: R) -> (JoinHandle<()>, Arc<Mutex<Vec<u8>>>) {
+/// A reader thread draining one pipe, plus the shared state the runner reads
+/// without joining it. `buf` accumulates the bytes; `eof` flips true only
+/// when the read hits a clean EOF (`Ok(0)` — every write end closed), so the
+/// runner can tell a fully-captured stream from one a descendant still holds
+/// open. Both are `Arc`s: a killed process's orphans inherit the write ends
+/// and would hold a join hostage, so the runner snapshots through the shared
+/// state and detaches a laggard rather than waiting it out.
+struct DrainHandle {
+    handle: JoinHandle<()>,
+    buf: Arc<Mutex<Vec<u8>>>,
+    eof: Arc<AtomicBool>,
+}
+
+/// Spawn a reader thread draining one output pipe into a shared buffer until
+/// EOF (write ends close at exit or kill), so neither pipe can fill and
+/// stall the child. A clean EOF sets the [`DrainHandle::eof`] flag; a read
+/// error breaks without setting it (the capture is not provably complete).
+fn drain<R: Read + Send + 'static>(pipe: R) -> DrainHandle {
     let buf = Arc::new(Mutex::new(Vec::new()));
+    let eof = Arc::new(AtomicBool::new(false));
     let sink = Arc::clone(&buf);
+    let eof_flag = Arc::clone(&eof);
     let handle = thread::spawn(move || {
         let mut pipe = pipe;
         let mut chunk = [0u8; 4096];
         loop {
             match pipe.read(&mut chunk) {
-                Ok(0) | Err(_) => break,
+                Ok(0) => {
+                    eof_flag.store(true, Ordering::SeqCst);
+                    break;
+                }
+                Err(_) => break,
                 Ok(n) => sink
                     .lock()
                     .expect("drain readers never panic holding the lock")
@@ -316,7 +408,7 @@ fn drain<R: Read + Send + 'static>(pipe: R) -> (JoinHandle<()>, Arc<Mutex<Vec<u8
             }
         }
     });
-    (handle, buf)
+    DrainHandle { handle, buf, eof }
 }
 
 /// Spawn `program args` with all three stdio streams piped, retrying only
@@ -355,7 +447,8 @@ fn spawn_piped(program: &Path, args: &[&OsStr]) -> std::io::Result<Child> {
 /// while pipes held open past the kill (an orphaned grandchild) cannot
 /// stall the runner. Mirrors `ckc_smt::verify`'s `run_process`, but the
 /// stdout stays raw bytes and a clean exit yields [`ModelOutcome::Completed`]
-/// rather than a parsed verdict.
+/// only when stdout reached EOF within the grace (else
+/// [`ModelOutcome::CaptureIncomplete`]), never a parsed verdict.
 fn run_process(program: &Path, args: &[&OsStr], stdin_bytes: &[u8], budget: Duration) -> ModelRun {
     let mut child = match spawn_piped(program, args) {
         Ok(child) => child,
@@ -375,8 +468,16 @@ fn run_process(program: &Path, args: &[&OsStr], stdin_bytes: &[u8], budget: Dura
     let stdin_thread = thread::spawn(move || {
         let _ = stdin.write_all(&payload);
     });
-    let (stdout_thread, stdout_buf) = drain(child.stdout.take().expect("stdout piped above"));
-    let (stderr_thread, stderr_buf) = drain(child.stderr.take().expect("stderr piped above"));
+    let DrainHandle {
+        handle: stdout_thread,
+        buf: stdout_buf,
+        eof: stdout_eof,
+    } = drain(child.stdout.take().expect("stdout piped above"));
+    let DrainHandle {
+        handle: stderr_thread,
+        buf: stderr_buf,
+        eof: _stderr_eof,
+    } = drain(child.stderr.take().expect("stderr piped above"));
 
     /// How the wait loop ended: a collected status, a budget kill, or a
     /// wait that itself failed (killed and folded into a codeless exit
@@ -422,11 +523,15 @@ fn run_process(program: &Path, args: &[&OsStr], stdin_bytes: &[u8], budget: Dura
     };
     let stdout_bytes = snapshot(&stdout_buf);
     let stderr = String::from_utf8_lossy(&snapshot(&stderr_buf)).into_owned();
+    let stdout_complete = stdout_eof.load(Ordering::SeqCst);
 
     let outcome = match fate {
         Fate::TimedOut => ModelOutcome::Timeout,
         Fate::WaitFailed => ModelOutcome::ExitFailure { code: None },
-        Fate::Exited(status) if status.success() => ModelOutcome::Completed {
+        Fate::Exited(status) if status.success() && stdout_complete => ModelOutcome::Completed {
+            bytes: stdout_bytes.clone(),
+        },
+        Fate::Exited(status) if status.success() => ModelOutcome::CaptureIncomplete {
             bytes: stdout_bytes.clone(),
         },
         Fate::Exited(status) => ModelOutcome::ExitFailure {
@@ -458,12 +563,14 @@ mod tests {
     }
 
     /// The committed model-runtime stub: a self-contained emulator of the
-    /// `model-adapter.1` CLI contract. `--identity` self-reports the three
-    /// identity fields; otherwise it honors `--constraint <path> --seed <n>`
-    /// with the prompt on stdin and writes deterministic bytes to stdout
-    /// (echoing the contract inputs). Two prompt sentinels drive the timeout
-    /// and nonzero-exit fates so every [`ModelOutcome`] is exercised from one
-    /// committed fixture.
+    /// CLI contract. `--identity` self-reports the three identity fields;
+    /// otherwise it honors `--constraint <path> --seed <n>` with the prompt
+    /// on stdin and writes deterministic bytes to stdout (echoing the
+    /// contract inputs). Three prompt sentinels drive the timeout,
+    /// nonzero-exit, and capture-incomplete fates — the last backgrounds a
+    /// `sleep` that inherits stdout and outlives the clean exit, holding the
+    /// pipe open past the drain grace — so every [`ModelOutcome`] is
+    /// exercised from one committed fixture.
     const COMMITTED_STUB: &str = r#"#!/bin/sh
 if [ "$1" = "--identity" ]; then
   echo "model_id=model.stub"
@@ -475,6 +582,7 @@ prompt=$(cat)
 case "$prompt" in
   __TIMEOUT__) sleep 30 ;;
   __EXIT_FAIL__) printf 'partial-out'; printf 'boom-err' 1>&2; exit 4 ;;
+  __CAPTURE_INCOMPLETE__) sleep 3 & printf 'partial' ;;
   *) printf 'constraint=%s seed=%s prompt=%s' "$2" "$4" "$prompt" ;;
 esac
 "#;
@@ -545,6 +653,58 @@ esac
         fs::remove_file(&path).unwrap();
     }
 
+    // Per-sample seed derivation: deterministic for a given (base, i),
+    // distinct across i and across base seeds, and collision-free over a
+    // short index run — the reproducible, decorrelated draw the k-sample set
+    // relies on.
+    #[test]
+    fn derive_seed_is_deterministic_and_distinct() {
+        assert_eq!(derive_seed(42, 3), derive_seed(42, 3));
+        assert_ne!(derive_seed(42, 0), derive_seed(42, 1));
+        assert_ne!(derive_seed(1, 5), derive_seed(2, 5));
+        let seeds: std::collections::BTreeSet<u64> = (0..16).map(|i| derive_seed(7, i)).collect();
+        assert_eq!(seeds.len(), 16, "derived seeds collided over 0..16");
+    }
+
+    // k-sample draw: k samples, each invoked under its [`derive_seed`] seed
+    // (echoed back by the stub) and `Completed`, the seeds distinct, the
+    // whole draw byte-reproducible from the same (base_seed, k), and the
+    // returned length the recorded-call count (zero for k == 0).
+    #[test]
+    fn invoke_samples_draws_k_reproducible_seeded_outputs() {
+        let path = committed_stub("ksample");
+        let adapter = ModelAdapter::with_command(&path).unwrap();
+        let constraint = Path::new("schemas/test.grammar");
+
+        let samples = adapter.invoke_samples("hi", constraint, 100, 4, Duration::from_secs(30));
+        assert_eq!(samples.len(), 4);
+        for (i, sample) in samples.iter().enumerate() {
+            assert_eq!(sample.seed, derive_seed(100, i as u32));
+            let expected = format!(
+                "constraint=schemas/test.grammar seed={} prompt=hi",
+                sample.seed
+            );
+            assert_eq!(sample.run.stdout_bytes, expected.as_bytes());
+            assert!(matches!(sample.run.outcome, ModelOutcome::Completed { .. }));
+        }
+        let seeds: std::collections::BTreeSet<u64> = samples.iter().map(|s| s.seed).collect();
+        assert_eq!(seeds.len(), 4, "k-sample seeds collided");
+
+        let again = adapter.invoke_samples("hi", constraint, 100, 4, Duration::from_secs(30));
+        assert_eq!(
+            again, samples,
+            "same (base_seed, k) must reproduce the draw"
+        );
+
+        assert!(
+            adapter
+                .invoke_samples("hi", constraint, 100, 0, Duration::from_secs(30))
+                .is_empty(),
+            "k == 0 is an empty draw"
+        );
+        fs::remove_file(&path).unwrap();
+    }
+
     // Budget kill: a stub generation outliving its budget is killed within
     // moments of the deadline and mints `Timeout` — the orphaned `sleep`
     // holding the stdout pipe open is detached after the drain grace, so the
@@ -579,6 +739,33 @@ esac
         fs::remove_file(&path).unwrap();
     }
 
+    // Clean exit, capture incomplete: a backgrounded descendant inherits
+    // stdout and outlives the exit-zero parent, holding the pipe open past
+    // the drain grace. The captured prefix is real but not provably whole,
+    // so the runner mints `CaptureIncomplete` rather than recording a
+    // truncated `Completed` — the EOF gate keeping byte-stable cassette
+    // bytes honest. Takes ~`DRAIN_GRACE` (the descendant never closes the
+    // pipe within it).
+    #[test]
+    fn clean_exit_holding_stdout_open_is_capture_incomplete() {
+        let path = committed_stub("captureincomplete");
+        let adapter = ModelAdapter::with_command(&path).unwrap();
+        let run = adapter.invoke(
+            "__CAPTURE_INCOMPLETE__",
+            Path::new("c"),
+            1,
+            Duration::from_secs(30),
+        );
+        assert_eq!(
+            run.outcome,
+            ModelOutcome::CaptureIncomplete {
+                bytes: b"partial".to_vec()
+            }
+        );
+        assert_eq!(run.stdout_bytes, b"partial");
+        fs::remove_file(&path).unwrap();
+    }
+
     // Spawn failure: a binary that vanishes after construction fails to
     // spawn, as data, with empty capture.
     #[test]
