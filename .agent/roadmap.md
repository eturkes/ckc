# CKC Roadmap

Phases contain top-level tasks; top-level tasks may contain indented subtasks.
See `.agent/prompt.md` for the session workflow that governs progression.

A trailing `(NNNK/200K)` on a completed subtask records the session's
context-window usage at completion, produced by `python3 .agent/context_window.py`
just before commit. Type A reads the prior top-level task's annotations to
calibrate new subtask granularity.

## Phase 0: Proof-Carrying Research Kernel

- [x] 0.1 Project scaffold
- [x] 0.2 CKC schema v0 (SPEC 10)
- [x] 0.3 Normal Form (SPEC 11)
- [x] 0.4 Content-addressed store
- [x] 0.5 Toy fixtures (SPEC 20 Phase 0 scenarios)
- [x] 0.6 Terminology and e-graph
- [x] 0.7 Retrieval fixture
- [ ] 0.8 Compiler targets (SPEC 14)
  - [x] 0.8.1 ckc-core compiler-contract types: add `crates/ckc-core/src/compile.rs` with `CompiledTarget` (fields: `target_language: TargetLanguage`, `artifact_text: String`, `compilation_map: CompilationMap`, `diagnostics: Vec<CompileDiagnostic>`, `source_artifact_hashes: Vec<ContentHash>`, `replay_command: String`, `target_parse_ok: Option<bool>`), `CompilationMap` (newtype over `Vec<SymbolMapping>`), `SymbolMapping` (`ckc_node_id: String`, `target_symbol: String`, `source_span_ids: Vec<SpanId>`), and `CompileDiagnostic` (`code: String`, `message_ja: String`, `message_en: String`, `source_span_ids: Vec<SpanId>`); add enum `TargetLanguage` (`SmtLib, Asp, Datalog, Lean, TlaPlus, Alloy`; `#[serde(rename_all="snake_case")]`) to `crates/ckc-core/src/enums.rs`. Derive Clone/Debug/PartialEq/Serialize/Deserialize/JsonSchema; add no `Normalize` impl (compile outputs are produced from an already-normalized bundle and are not re-normalized). Keep `certificate_class` OUT of `CompiledTarget` — certificates are assigned in 0.9 via `Certificate.certificate_class` referencing this artifact's hash. Add `pub mod compile;` + re-exports to `crates/ckc-core/src/lib.rs`; add a `CompiledTarget` variant to `ArtifactKind` in `crates/ckc-core/src/envelope.rs`. Add golden suites (canonical/roundtrip/schema, via the existing `golden_suite!` macro) for `CompiledTarget`, `CompilationMap`, and `CompileDiagnostic` in `crates/ckc-core/tests/golden.rs` with fixture builder fns, then regenerate `schemas/golden/*.json` + `schemas/*.schema.json` (including `artifact_kind.schema.json` for the new variant) via the `--ignored` regenerate test. Gate: `cargo test -p ckc-core` passes (new golden suites green; artifact_kind schema regenerated); all workspace tests pass. (118K/200K)
  - [x] 0.8.2 ckc-compile crate scaffold + Compiler trait + bundle loader: create `crates/ckc-compile/` and add it to root `Cargo.toml` `[workspace] members`; Cargo.toml deps `ckc-core = { path = "../ckc-core" }` + serde/serde_json/schemars/anyhow `{ workspace = true }`, dev-deps `ckc-store = { path = "../ckc-store" }` + `tempfile = { workspace = true }`. In `src/lib.rs` define `CompileBundle { rules: Vec<Rule>, concepts: Vec<Concept>, decision_tables: Vec<DecisionTable>, event_narratives: Vec<EventNarrative>, conflicts: Vec<Conflict>, spans: Vec<SourceSpan> }` with `CompileBundle::load_toy()` using `include_str!` over `examples/research_kernel/fixtures/{rules,concepts,decision_tables,event_narratives,conflicts,spans}.json`. Consume the fixtures as committed: their readable ids (e.g. `rule_id` `"rule_sepsis_bl_recommend"`) ARE the CKC node ids used in every CompilationMap; preserve them rather than re-running NF Pass-12 stable-id rewriting (which would desync cross-references in conflicts/claims). Define `pub trait Compiler { fn target_language(&self) -> TargetLanguage; fn compile(&self, bundle: &CompileBundle) -> CompiledTarget; }` and a `replay_command(lang: TargetLanguage) -> String` helper. `tests/scaffold.rs`: `load_toy()` parses all six fixtures with expected counts (3 rules, 10 concepts, 1 decision table, 1 event narrative, 3 conflicts, 16 spans). Gate: `cargo test -p ckc-compile` passes; crate builds in the workspace; all workspace tests pass. (102K/200K)
  - [x] 0.8.3 Emitter support helpers (crate-level): in `crates/ckc-compile/src/lib.rs` add `pub fn build_target(target_language: TargetLanguage, artifact_text: String, compilation_map: CompilationMap, source_artifact_hashes: Vec<ContentHash>) -> CompiledTarget` setting `replay_command = replay_command(target_language)`, `diagnostics = vec![]`, `target_parse_ok = None`; and `pub fn sorted_lines(lines: Vec<String>) -> String` that lexicographically sorts `lines`, joins with `'\n'`, and appends one trailing `'\n'` (the deterministic block builder every text emitter reuses for declaration/fact blocks). Re-export `CompilationMap`, `SymbolMapping`, `CompiledTarget`, `TargetLanguage` and `ckc_core::canonical::{ContentHash, content_hash}` from the crate root for emitter/test ergonomics. `tests/support.rs`: assert `sorted_lines(vec!["b","a"])` == `"a\nb\n"` and is order-independent (two different input orders → equal output); assert `build_target(...)` fills `replay_command` from `replay_command`, leaves `diagnostics` empty, and sets `target_parse_ok` None. Gate: `cargo test -p ckc-compile` support tests pass; all workspace tests pass. (86K/200K)
  - [x] 0.8.4 SMT-LIB norm-conflict emitter: add `crates/ckc-compile/src/smt.rs` (`pub mod smt;`) with `pub fn emit_norm_conflict(bundle: &CompileBundle) -> CompiledTarget`; read `crates/ckc-core/src/clinical.rs` (`Rule`, `Norm`) for field names. Encode the deontic conflict between `rule_sepsis_bl_recommend` (`norm.direction=for`, `norm.deontic_projection=recommended`) and `rule_bl_anaphylaxis_contra` (`norm.direction=against`, `norm.deontic_projection=prohibited`) over the shared context `(dx sepsis) AND (allergy_history beta_lactam anaphylaxis)` of `conflict_norm_bl_contradiction`: `(set-logic QF_UF)`; `declare-const` Bool for context atoms `dx_sepsis`, `allergy_history_beta_lactam_anaphylaxis` and for `recommend_administer_beta_lactam`/`prohibit_administer_beta_lactam` (action target `concept_beta_lactam`); assert each norm fires under its context and that `(and recommend_administer_beta_lactam prohibit_administer_beta_lactam)` is unsatisfiable absent priority; `(check-sat)`. Build the declaration block with `sorted_lines`; keep asserts in fixed code order. `compilation_map`: `rule_sepsis_bl_recommend` → `recommend_administer_beta_lactam` (spans `span_rec_sepsis_bl`, `span_evidence_sepsis`), `rule_bl_anaphylaxis_contra` → `prohibit_administer_beta_lactam` (spans `span_contra_bl_allergy`, `span_allergy_history`); `source_artifact_hashes` = `content_hash` of the two consumed `Rule`s. Emit-only (no z3/cvc5; solver run is 0.9). `tests/smt_norm_conflict.rs` (no committed golden — full-bytes locking is consolidated in 0.8.14/0.8.15): two `emit_norm_conflict` calls → equal `content_hash` (determinism); `artifact_text` contains `(set-logic QF_UF)` and `(check-sat)`; `compilation_map` contains both rule→symbol entries with their spans. Gate: `cargo test -p ckc-compile` smt_norm_conflict tests pass; byte-identical across two compiles; all workspace tests pass. (111K/200K)
  - [ ] 0.8.5 SMT-LIB decision-table emitter: in `crates/ckc-compile/src/smt.rs` add `pub fn emit_decision_table(bundle: &CompileBundle) -> CompiledTarget` over `dt_vitals_triage` (`hit_policy=unique`); read `crates/ckc-core/src/artifact.rs` (`DecisionTable`, `DecisionRow`) for fields. `(set-logic QF_LRA)`; declare Real consts named from each condition's `field` (`temperature`, `heart_rate`, `systolic_bp`); encode row predicates `row_temp_high` (temperature ≥ 38.0), `row_temp_very_high` (temperature ≥ 38.5), `row_hr_high` (heart_rate > 90), `row_bp_low` (systolic_bp < 90); assert the unique-policy overlap witness (temperature ≥ 38.5 satisfies both `row_temp_high` and `row_temp_very_high`, whose outputs `administer_antipyretic`/`initiate_cooling` differ) and the gap witness (`temperature=37.5 ∧ heart_rate=85 ∧ systolic_bp=95` matches no row); `(check-sat)`. `sorted_lines` for declarations; fixed-order asserts. `compilation_map`: each `row_id` → its predicate symbol with the row's `source_span_ids` (`span_cell_r0c0`/`span_cell_r0c1` … `span_cell_r3c0`/`span_cell_r3c1`); `source_artifact_hashes` = `content_hash` of the `DecisionTable`. Emit-only. `tests/smt_decision_table.rs`: determinism (two calls → equal `content_hash`); `artifact_text` contains `(set-logic QF_LRA)`; `compilation_map` has all four row entries. Gate: `cargo test -p ckc-compile` smt_decision_table tests pass; byte-identical across compiles; all workspace tests pass.
  - [ ] 0.8.6 SMT-LIB MaxSMT repair emitter: in `crates/ckc-compile/src/smt.rs` add `pub fn emit_repair_maxsmt(bundle: &CompileBundle) -> CompiledTarget` for `conflict_norm_bl_contradiction`; read its two `repair_candidates` from the `Conflict` (`crates/ckc-core/src/verify.rs`): `add_priority` (`superior=rule_bl_anaphylaxis_contra` ≻ `rule_sepsis_bl_recommend`) and `add_exception` (`rule=rule_sepsis_bl_recommend`, `exception=beta_lactam_anaphylaxis`). Reuse the norm-conflict hard constraints; add `(assert-soft repair_add_priority …)` and `(assert-soft repair_add_exception …)` plus a weight so a minimum-cost model selects exactly one repair; end with `(check-sat)` `(get-objectives)` (Z3 Optimize dialect). `compilation_map`: each repair candidate → its soft-constraint symbol (`repair_add_priority`, `repair_add_exception`) carrying the conflict `source_spans` (`span_rec_sepsis_bl`, `span_contra_bl_allergy`); `source_artifact_hashes` = `content_hash` of the `Conflict`. Emit-only (Optimize run is 0.9). `tests/smt_repair.rs`: determinism; both repair symbols present in `compilation_map`; `artifact_text` contains `assert-soft` and `(get-objectives)`. Gate: `cargo test -p ckc-compile` smt_repair tests pass; byte-identical across compiles; all workspace tests pass.
  - [ ] 0.8.7 ASP defeasible + argumentation emitter: add `crates/ckc-compile/src/asp.rs` (`pub mod asp;`) with `pub fn emit_defeasible(bundle: &CompileBundle) -> CompiledTarget`. `CompileBundle` carries no argument graph, so derive the Dung view from the rules. Emit a Clingo program from the two defeasible `Rule`s and their superiority (`rule_bl_anaphylaxis_contra.priority_over ∋ rule_sepsis_bl_recommend`): facts `applicable(rule_sepsis_bl_recommend).`, `applicable(rule_bl_anaphylaxis_contra).`, `superior(rule_bl_anaphylaxis_contra,rule_sepsis_bl_recommend).`; a `defeated(R) :- …` derivation; derived arguments `argument(arg_rule_sepsis_bl_recommend).`, `argument(arg_rule_bl_anaphylaxis_contra).` and `attacks(arg_rule_bl_anaphylaxis_contra,arg_rule_sepsis_bl_recommend).` from the superiority, with grounded-extension constraints (`in/1`, `defeated_arg/1`). Build fact/rule blocks with `sorted_lines`. `compilation_map`: each `rule_id` → its `applicable`/`argument` atom with the rule's `source_span_ids`; `source_artifact_hashes` = `content_hash` of the two `Rule`s. Emit-only (clingo run is 0.9). `tests/asp_defeasible.rs`: determinism; `artifact_text` contains `superior(rule_bl_anaphylaxis_contra,rule_sepsis_bl_recommend).` and an `attacks(` atom. Gate: `cargo test -p ckc-compile` asp_defeasible tests pass; byte-identical across compiles; all workspace tests pass.
  - [ ] 0.8.8 ASP Event Calculus emitter: in `crates/ckc-compile/src/asp.rs` add `pub fn emit_event_calculus(bundle: &CompileBundle) -> CompiledTarget` over the single `EventNarrative` (`crates/ckc-core/src/artifact.rs`). Emit a Clingo Event Calculus program with persistence/clipping axioms (`holdsAt`, `happens`, `initiates`, `terminates`, `stoppedIn`): `happens(detect_allergy,0).`, `happens(administer_drug,10).`, `initiates(detect_allergy,allergy_known,0).`, `initiates(administer_drug,drug_active,10).`, no `terminates` for `allergy_known` so `holdsAt(allergy_known,10)` derives; plus the violation query that `administer_drug` at 10 fires while `holdsAt(allergy_known,10)` (`conflict_ec_allergy_persistence`). `sorted_lines` for the fact block. `compilation_map`: event/fluent names (`detect_allergy`, `administer_drug`, `allergy_known`, `drug_active`) → their ASP atoms with the narrative `source_span_ids` (`span_allergy_history`, `span_contra_bl_allergy`); `source_artifact_hashes` = `content_hash` of the `EventNarrative`. Emit-only. `tests/asp_event_calculus.rs`: determinism; `artifact_text` contains `holdsAt(allergy_known,10)` and `happens(administer_drug,10).`. Gate: `cargo test -p ckc-compile` asp_event_calculus tests pass; byte-identical across compiles; all workspace tests pass.
  - [ ] 0.8.9 Datalog/Soufflé priority-cycle emitter: add `crates/ckc-compile/src/datalog.rs` (`pub mod datalog;`) with `pub fn emit_priority_analysis(bundle: &CompileBundle) -> CompiledTarget`. Emit a Soufflé `.dl` program: `.decl priority_over(sup symbol, inf symbol)` with one input fact per rule `priority_over` entry (`priority_over("rule_bl_anaphylaxis_contra","rule_sepsis_bl_recommend").`), a transitive-closure relation `reaches`, `.decl cycle(r symbol)` deriving `reaches(r,r)` (empty for the toy set → witnessed acyclicity), and `.output cycle`. `sorted_lines` for the fact block. `compilation_map`: each `rule_id` appearing in a `priority_over` → its Datalog symbol with that rule's `source_span_ids`; `source_artifact_hashes` = `content_hash` of the rules carrying `priority_over`. Emit-only; the PATH-guarded `souffle` parse-check is deferred to 0.9 so solver-less containers stay green. `tests/datalog_priority.rs`: determinism; `artifact_text` contains `.decl priority_over` and `.output cycle`. Gate: `cargo test -p ckc-compile` datalog_priority tests pass; byte-identical across compiles; all workspace tests pass.
  - [ ] 0.8.10 Lean theorem-file emitter: add `crates/ckc-compile/src/lean.rs` (`pub mod lean;`) with `pub fn emit_norm_conflict_theorem(bundle: &CompileBundle) -> CompiledTarget` producing Lean 4 source (target file `lean/Ckc/NormConflict.lean`): an inductive `Deontic` (`recommend | prohibit`), the two norms over a shared context modeled as propositions, and `theorem unprioritized_norm_conflict : ¬ (recommend ∧ prohibit)` closed by a proof term (`by tauto`/`by simp`/`by decide`) so the file carries no `sorry`/`admit`. `compilation_map`: each `rule_id` → its Lean identifier with the rule's `source_span_ids`; `source_artifact_hashes` = `content_hash` of the two `Rule`s. Emit-only (`lake build` kernel check is 0.9). `tests/lean_theorem.rs`: determinism; assert `artifact_text` excludes the substrings `sorry` and `admit`; `artifact_text` contains `theorem unprioritized_norm_conflict`. Gate: `cargo test -p ckc-compile` lean_theorem tests pass; emitted Lean is sorry/admit-free; byte-identical across compiles; all workspace tests pass.
  - [ ] 0.8.11 TLA+ stub-spec emitter: add `crates/ckc-compile/src/modelcheck.rs` (`pub mod modelcheck;`) with `pub fn emit_tlaplus_stub(bundle: &CompileBundle) -> CompiledTarget`. Emit a generated TLA+ meta-spec stub (target file `logic/tla/Conflict.tla`): `---- MODULE Conflict ----` header, `VARIABLES allergy_known, drug_active`, an `Init`/`Next` skeleton, and an invariant `AllergyContraindication == allergy_known => ~drug_active` reflecting `conflict_ec_allergy_persistence` — stub only, no TLC/Apalache. `compilation_map`: fluent names (`allergy_known`, `drug_active`) → TLA+ variables with the `EventNarrative` `source_span_ids`; `source_artifact_hashes` = `content_hash` of the `EventNarrative`. `tests/modelcheck_tla.rs`: determinism; `artifact_text` contains `MODULE Conflict` and `VARIABLES`. Gate: `cargo test -p ckc-compile` modelcheck_tla tests pass; byte-identical across compiles; all workspace tests pass.
  - [ ] 0.8.12 Alloy stub-spec emitter: in `crates/ckc-compile/src/modelcheck.rs` add `pub fn emit_alloy_stub(bundle: &CompileBundle) -> CompiledTarget`. Emit an Alloy stub (target file `logic/alloy/Priority.als`): `sig Rule {}` with a `priority_over: set Rule` field, a fact populating the toy superiority (`rule_bl_anaphylaxis_contra` → `rule_sepsis_bl_recommend`), and `check NoPriorityCycle { no r: Rule | r in r.^priority_over }` — stub only, no Alloy Analyzer run. `compilation_map`: each `rule_id` in a `priority_over` → its Alloy atom with the rule's `source_span_ids`; `source_artifact_hashes` = `content_hash` of those rules. `tests/modelcheck_alloy.rs`: determinism; `artifact_text` contains `sig Rule` and `check NoPriorityCycle`. Gate: `cargo test -p ckc-compile` modelcheck_alloy tests pass; byte-identical across compiles; all workspace tests pass.
  - [ ] 0.8.13 Compiler portfolio orchestration: in `crates/ckc-compile/src/lib.rs` add `pub fn compile_all(bundle: &CompileBundle) -> Vec<CompiledTarget>` invoking every emitter in a fixed deterministic order: `smt::emit_norm_conflict`, `smt::emit_decision_table`, `smt::emit_repair_maxsmt`, `asp::emit_defeasible`, `asp::emit_event_calculus`, `datalog::emit_priority_analysis`, `lean::emit_norm_conflict_theorem`, `modelcheck::emit_tlaplus_stub`, `modelcheck::emit_alloy_stub` (9 targets). `tests/portfolio.rs`: assert `compile_all(&CompileBundle::load_toy())` returns 9 targets whose `target_language` sequence matches that order (SMT and ASP repeat), every `CompiledTarget.compilation_map` is non-empty, and two `compile_all` runs → equal per-index `content_hash` vector. No file writing or committed golden here. Gate: `cargo test -p ckc-compile` portfolio tests pass; all workspace tests pass.
  - [ ] 0.8.14 Committed target-artifact files + byte-identical regeneration: create `logic/{smt,asp,datalog,tla,alloy}/` and `lean/Ckc/`; add an `#[ignore = "regenerate-only"]` writer test in `tests/portfolio.rs` (mirror the ckc-retrieve regenerate pattern) that writes each `compile_all` target's `artifact_text` to its canonical path — `logic/smt/norm_conflict.smt2`, `logic/smt/decision_table.smt2`, `logic/smt/repair_maxsmt.smt2`, `logic/asp/defeasible.lp`, `logic/asp/event_calculus.lp`, `logic/datalog/priority.dl`, `lean/Ckc/NormConflict.lean`, `logic/tla/Conflict.tla`, `logic/alloy/Priority.als` — and commit those nine files. Add a non-ignored gate test asserting each committed file's bytes equal the corresponding `compile_all` target's `artifact_text` (regeneration is byte-identical). Gate: `cargo test -p ckc-compile` file-equality test passes; committed `logic/*` + `lean/Ckc/*` files regenerate byte-identically; all workspace tests pass.
  - [ ] 0.8.15 Portfolio-manifest golden: in `crates/ckc-compile/src/lib.rs` define `PortfolioEntry { target_language: TargetLanguage, artifact_path: String, content_hash: ContentHash }` and `PortfolioManifest(pub Vec<PortfolioEntry>)` (derive Clone/Debug/PartialEq/Serialize/Deserialize/JsonSchema), plus `pub fn portfolio_manifest(bundle: &CompileBundle) -> PortfolioManifest` pairing each `compile_all` target (in order) with its `logic/`/`lean/` path (from 0.8.14) and the `content_hash` of the `CompiledTarget`. `tests/manifest.rs`: canonical/roundtrip/schema golden (reuse the `golden_suite!`-style helpers) writing `schemas/golden/compile_portfolio_manifest.json` + `schemas/compile_portfolio_manifest.schema.json` via an `#[ignore]` regenerate test; this one compact golden byte-locks every target through its hash. Gate: `cargo test -p ckc-compile` manifest tests pass; manifest golden matches; all workspace tests pass.
  - [ ] 0.8.16 CompiledTarget CAS persistence + cross-run determinism: in `crates/ckc-compile/tests/persistence.rs` run `compile_all(&CompileBundle::load_toy())`, wrap each `CompiledTarget` via `ArtifactEnvelope::wrap(ArtifactKind::CompiledTarget, &t, meta)` (meta stage `"compile"`, `semantic_profiles` reflecting each target's source profiles), and `ContentStore::put_batch` into a `tempfile::TempDir`; assert each artifact `exists` and `verify`s. Run the load→compile_all→wrap pipeline twice over two independent temp stores and assert per-`CompiledTarget` `envelope_hash`/`content_hash` match across runs — compare per-artifact hashes, not raw `StoreManifest` bytes (`stored_at_epoch` differs by design; mirror ckc-store `cas_manifest_hash_is_stable` and `crates/ckc-retrieve/tests/persistence.rs`). Gate: `cargo test -p ckc-compile` persistence tests pass; every CompiledTarget stored/verified/content-hashed; per-artifact hashes match across two runs; `scripts/ci.sh` green; all workspace tests pass.
- [ ] 0.9 Verification and certificates (SPEC 13)
- [ ] 0.10 Conflict detection (SPEC 15)
- [ ] 0.11 CLI commands (SPEC 18)
- [ ] 0.12 Report and UI
- [ ] 0.13 Replay and determinism
