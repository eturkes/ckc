# CKC Roadmap

Phases contain top-level tasks; top-level tasks may contain indented subtasks.
See `.agent/prompt.md` for the session workflow that governs progression.

A trailing `(NNNK/200K)` on a completed subtask records the session's
context-window usage at completion, produced by `python3 .agent/context_window.py`
just before commit. Type A reads the prior top-level task's annotations to
calibrate new subtask granularity.

## Phase 0: Proof-Carrying Research Kernel

- [x] 0.1 Project scaffold
- [x] 0.2 CKC schema v0 (SPEC 10)
- [x] 0.3 Normal Form (SPEC 11)
- [x] 0.4 Content-addressed store
- [x] 0.5 Toy fixtures (SPEC 20 Phase 0 scenarios)
- [x] 0.6 Terminology and e-graph
- [x] 0.7 Retrieval fixture
- [ ] 0.8 Compiler targets (SPEC 14)
  - [ ] 0.8.1 ckc-core compiler-contract types: add `crates/ckc-core/src/compile.rs` with `CompiledTarget` (fields: `target_language: TargetLanguage`, `artifact_text: String`, `compilation_map: CompilationMap`, `diagnostics: Vec<CompileDiagnostic>`, `source_artifact_hashes: Vec<ContentHash>`, `replay_command: String`, `target_parse_ok: Option<bool>`), `CompilationMap` (newtype over `Vec<SymbolMapping>`), `SymbolMapping` (`ckc_node_id: String`, `target_symbol: String`, `source_span_ids: Vec<SpanId>`), and `CompileDiagnostic` (`code: String`, `message_ja: String`, `message_en: String`, `source_span_ids: Vec<SpanId>`); add enum `TargetLanguage` (`SmtLib, Asp, Datalog, Lean, TlaPlus, Alloy`; `#[serde(rename_all="snake_case")]`) to `crates/ckc-core/src/enums.rs`. Derive Clone/Debug/PartialEq/Serialize/Deserialize/JsonSchema; add no `Normalize` impl (compile outputs are produced from an already-normalized bundle and are not re-normalized). Keep `certificate_class` OUT of `CompiledTarget` — certificates are assigned in 0.9 via `Certificate.certificate_class` referencing this artifact's hash. Add `pub mod compile;` + re-exports to `crates/ckc-core/src/lib.rs`; add a `CompiledTarget` variant to `ArtifactKind` in `crates/ckc-core/src/envelope.rs`. Add golden suites (canonical/roundtrip/schema, via the existing `golden_suite!` macro) for `CompiledTarget`, `CompilationMap`, and `CompileDiagnostic` in `crates/ckc-core/tests/golden.rs` with fixture builder fns, then regenerate `schemas/golden/*.json` + `schemas/*.schema.json` (including `artifact_kind.schema.json` for the new variant) via the `--ignored` regenerate test. Gate: `cargo test -p ckc-core` passes (new golden suites green; artifact_kind schema regenerated); all workspace tests pass.
  - [ ] 0.8.2 ckc-compile crate scaffold + Compiler trait + bundle loader: create `crates/ckc-compile/` and add it to root `Cargo.toml` `[workspace] members`; Cargo.toml deps `ckc-core = { path = "../ckc-core" }` + serde/serde_json/schemars/anyhow `{ workspace = true }`, dev-deps `ckc-store = { path = "../ckc-store" }` + `tempfile = { workspace = true }`. In `src/lib.rs` define `CompileBundle { rules: Vec<Rule>, concepts: Vec<Concept>, decision_tables: Vec<DecisionTable>, event_narratives: Vec<EventNarrative>, conflicts: Vec<Conflict>, spans: Vec<SourceSpan> }` with `CompileBundle::load_toy()` using `include_str!` over `examples/research_kernel/fixtures/{rules,concepts,decision_tables,event_narratives,conflicts,spans}.json`. Consume the fixtures as committed: their readable ids (e.g. `rule_id` `"rule_sepsis_bl_recommend"`) ARE the CKC node ids used in every CompilationMap; preserve them rather than re-running NF Pass-12 stable-id rewriting (which would desync cross-references in conflicts/claims). Define `pub trait Compiler { fn target_language(&self) -> TargetLanguage; fn compile(&self, bundle: &CompileBundle) -> CompiledTarget; }` and a `replay_command(lang: TargetLanguage) -> String` helper. `tests/scaffold.rs`: `load_toy()` parses all six fixtures with expected counts (3 rules, 10 concepts, 1 decision table, 1 event narrative, 3 conflicts, 16 spans). Gate: `cargo test -p ckc-compile` passes; crate builds in the workspace; all workspace tests pass.
  - [ ] 0.8.3 SMT-LIB norm-conflict emitter: add `crates/ckc-compile/src/smt.rs` (`pub mod smt;`) with `emit_norm_conflict(bundle: &CompileBundle) -> CompiledTarget`. Encode the deontic conflict between `rule_sepsis_bl_recommend` (`direction=for`, `deontic_projection=recommended`) and `rule_bl_anaphylaxis_contra` (`direction=against`, `deontic_projection=prohibited`) over the satisfiable shared context from `conflict_norm_bl_contradiction` (`"(dx sepsis) AND (allergy_history beta_lactam anaphylaxis)"`): declare Bool consts for the context atoms and for recommend/prohibit of action `administer beta_lactam` (`target_concept concept_beta_lactam`); assert both norms fire and that `recommend ∧ prohibit` is unsatisfiable absent priority. Emit deterministic SMT-LIB text (`(set-logic QF_UF)`, lexicographically sorted declarations, `(check-sat)`). CompilationMap maps each `rule_id` → its SMT symbol with `source_span_ids` (`span_rec_sepsis_bl`, `span_contra_bl_allergy`, `span_allergy_history`). Emit-only — no z3/cvc5 invocation (solver run is 0.9). `tests/smt_norm_conflict.rs`: golden canonical-bytes check of the `CompiledTarget` against `schemas/golden/`, plus repeated-compilation identity (two `compile()` calls → equal `content_hash`). Gate: `cargo test -p ckc-compile` smt_norm_conflict tests pass; emitted SMT-LIB is byte-identical across runs; all workspace tests pass.
  - [ ] 0.8.4 SMT-LIB decision-table emitter: in `crates/ckc-compile/src/smt.rs` add `emit_decision_table(bundle) -> CompiledTarget` over decision table `dt_vitals_triage` (`hit_policy=unique`). Declare Real vars for 体温/心拍数/収縮期血圧 (concepts `concept_body_temperature`/`concept_heart_rate`/`concept_blood_pressure`); encode each row condition as Bool (`row_temp_high` temp≥38.0, `row_temp_very_high` temp≥38.5, `row_hr_high` hr>90, `row_bp_low` sbp<90); assert the unique-policy overlap witness (temp≥38.5 fires both `row_temp_high` and `row_temp_very_high` with incompatible outputs `administer_antipyretic` vs `initiate_cooling`) and the gap witness (temp=37.5 ∧ hr=85 ∧ sbp=95 matches no row). Emit `(set-logic QF_LRA)`, sorted declarations, deterministic text. CompilationMap maps each `row_id` → SMT symbol with cell spans (`span_cell_r0c0`..`span_cell_r3c0`). Emit-only. `tests/smt_decision_table.rs`: golden + repeated-compilation identity. Gate: `cargo test -p ckc-compile` smt_decision_table tests pass; byte-identical across runs; all workspace tests pass.
  - [ ] 0.8.5 SMT-LIB MaxSMT repair emitter: in `crates/ckc-compile/src/smt.rs` add `emit_repair_maxsmt(bundle) -> CompiledTarget` for `conflict_norm_bl_contradiction`. Reuse the norm-conflict hard constraints and add soft constraints (Z3 `(assert-soft ...)` + `(maximize ...)`) over the two repair candidates in `conflicts.json` (add_priority: `rule_bl_anaphylaxis_contra` ≻ `rule_sepsis_bl_recommend`; add_exception: `rule_sepsis_bl_recommend` + exception `beta_lactam_anaphylaxis`), so a minimal-cost model selects exactly one repair; end with `(check-sat)` + `(get-objectives)`. Emit deterministic text (Z3 Optimize dialect). CompilationMap maps each repair candidate → its soft-constraint symbol. Emit-only (Z3 Optimize run is 0.9). `tests/smt_repair.rs`: golden + repeated-compilation identity; assert both repair symbols are present in `compilation_map`. Gate: `cargo test -p ckc-compile` smt_repair tests pass; byte-identical across runs; all workspace tests pass.
  - [ ] 0.8.6 ASP defeasible + argumentation emitter: add `crates/ckc-compile/src/asp.rs` (`pub mod asp;`) with `emit_defeasible(bundle) -> CompiledTarget`. Emit a Clingo program encoding the two defeasible rules and their superiority (`rule_bl_anaphylaxis_contra.priority_over = ["rule_sepsis_bl_recommend"]`): facts `applicable(rule_sepsis_bl_recommend).`, `applicable(rule_bl_anaphylaxis_contra).`, `superior(rule_bl_anaphylaxis_contra, rule_sepsis_bl_recommend).`; `defeated/1` derivation rules; and a Dung-style argument graph (`argument/1`, `attacks/2`) mirroring `argument_graphs.json` `ag_sepsis_bl_conflict` (`arg_contraindicate_bl` rebuts `arg_recommend_bl`) with grounded-extension constraints. Deterministic, lexicographically sorted output. CompilationMap maps rule ids and argument ids → ASP atoms with source spans. Emit-only (clingo run is 0.9; clingo is not installed). `tests/asp_defeasible.rs`: golden + repeated-compilation identity. Gate: `cargo test -p ckc-compile` asp_defeasible tests pass; byte-identical across runs; all workspace tests pass.
  - [ ] 0.8.7 ASP Event Calculus emitter: in `crates/ckc-compile/src/asp.rs` add `emit_event_calculus(bundle) -> CompiledTarget` over `event_narratives.json`. Emit a Clingo Event Calculus program (`happens/2`, `initiates/3`, `terminates/3`, `holdsAt/2`, with persistence/clipping axioms): `happens(detect_allergy,0)`, `happens(administer_drug,10)`, `initiates(detect_allergy, allergy_known, 0)`, `initiates(administer_drug, drug_active, 10)`, no `terminates` for `allergy_known` → `holdsAt(allergy_known,10)`; plus the violation query that `administer_drug` at t=10 occurs while `holdsAt(allergy_known,10)` (`conflict_ec_allergy_persistence`). Deterministic sorted output. CompilationMap maps event/fluent names → ASP atoms with source spans `span_allergy_history`, `span_contra_bl_allergy`. Emit-only. `tests/asp_event_calculus.rs`: golden + repeated-compilation identity. Gate: `cargo test -p ckc-compile` asp_event_calculus tests pass; byte-identical across runs; all workspace tests pass.
  - [ ] 0.8.8 Datalog/Soufflé static-analysis emitter: add `crates/ckc-compile/src/datalog.rs` (`pub mod datalog;`) with `emit_priority_analysis(bundle) -> CompiledTarget`. Emit a Soufflé `.dl` program: `.decl priority_over(sup symbol, inf symbol)` with input facts from every rule's `priority_over` (`rule_bl_anaphylaxis_contra` → `rule_sepsis_bl_recommend`), a transitive-closure relation `reaches`, and a `.decl cycle(r symbol)` rule flagging priority cycles (none in the toy set → empty `cycle` relation, witnessing acyclicity). Deterministic sorted output. CompilationMap maps `rule_id`s → Datalog symbols. Optional target-parse: when `souffle` is on PATH (guard with `command -v souffle`), run a parse-only check and record `target_parse_ok`; set `target_parse_ok = None` and skip gracefully when absent so CI on solver-less containers stays green. `tests/datalog_priority.rs`: golden + repeated-compilation identity. Gate: `cargo test -p ckc-compile` datalog_priority tests pass; byte-identical across runs; all workspace tests pass.
  - [ ] 0.8.9 Lean theorem-file emitter: add `crates/ckc-compile/src/lean.rs` (`pub mod lean;`) with `emit_norm_conflict_theorem(bundle) -> CompiledTarget` producing a Lean 4 source (shape of `lean/Ckc/NormConflict.lean`): declare an inductive `Deontic` (`recommend | prohibit`), model the two norms over a shared context, and state `theorem unprioritized_norm_conflict : ¬ (recommend ∧ prohibit)` (or a decision-table uniqueness statement) together with a closeable proof term (e.g. `by tauto` / `by simp` / `by decide`) so the emitted file is free of `sorry` and `admit`. The Lean kernel check (`lake build`) is 0.9; lean 4.30 is installed but 0.8 stays emit-only (an optional `lean --version` guard may set `target_parse_ok`). CompilationMap maps `rule_id`s → Lean identifiers with source spans. `tests/lean_theorem.rs`: golden + repeated-compilation identity; assert the emitted text excludes the tokens `sorry` and `admit`. Gate: `cargo test -p ckc-compile` lean_theorem tests pass; byte-identical across runs; emitted Lean is sorry/admit-free; all workspace tests pass.
  - [ ] 0.8.10 TLA+/Alloy stub-spec emitters: add `crates/ckc-compile/src/modelcheck.rs` (`pub mod modelcheck;`) with `emit_tlaplus_stub(bundle) -> CompiledTarget` and `emit_alloy_stub(bundle) -> CompiledTarget`. TLA+: emit a generated meta-spec stub (MODULE header, `VARIABLES allergy_known, drug_active`, an `Init`/`Next` skeleton, and an invariant `allergy_known => ~administer` reflecting `conflict_ec_allergy_persistence`) — stub only, no TLC/Apalache run. Alloy: emit a stub (`sig`/`fact` skeleton for Rule/priority and a `check` asserting no priority cycle) — stub only. Deterministic sorted output; each CompilationMap maps source ids → spec identifiers. `tests/modelcheck_stubs.rs`: golden + repeated-compilation identity for both targets. Gate: `cargo test -p ckc-compile` modelcheck_stubs tests pass; byte-identical across runs; all workspace tests pass.
  - [ ] 0.8.11 Compiler portfolio orchestration: in `crates/ckc-compile/src/lib.rs` add `compile_all(bundle: &CompileBundle) -> Vec<CompiledTarget>` invoking every emitter in a fixed deterministic order (smt norm-conflict, smt decision-table, smt repair, asp defeasible, asp event-calculus, datalog priority, lean theorem, tlaplus stub, alloy stub). Create `logic/{smt,asp,datalog,tla,alloy}/` and `lean/Ckc/` and write each `CompiledTarget.artifact_text` to its canonical file (`logic/smt/norm_conflict.smt2`, `logic/smt/decision_table.smt2`, `logic/smt/repair_maxsmt.smt2`, `logic/asp/defeasible.lp`, `logic/asp/event_calculus.lp`, `logic/datalog/priority.dl`, `lean/Ckc/NormConflict.lean`, `logic/tla/Conflict.tla`, `logic/alloy/Priority.als`) via an `#[ignore = "regenerate-only"]` writer test (mirror the ckc-retrieve regenerate pattern); commit those files. `tests/portfolio.rs`: assert `compile_all` returns the expected target set (one per emitter), golden canonical-bytes check over the ordered `Vec<(TargetLanguage, ContentHash)>` portfolio manifest stored in `schemas/golden/`, and that every `CompiledTarget` has a non-empty `compilation_map`. Gate: `cargo test -p ckc-compile` portfolio tests pass; portfolio-manifest golden matches; committed logic/lean files regenerate byte-identically; all workspace tests pass.
  - [ ] 0.8.12 CompiledTarget CAS persistence + cross-run determinism: in `crates/ckc-compile/tests/persistence.rs` run `compile_all(CompileBundle::load_toy())`, wrap each `CompiledTarget` via `ArtifactEnvelope::wrap(ArtifactKind::CompiledTarget, &t, meta)` (meta stage `"compile"`, `semantic_profiles` reflecting each target's source profiles), and `ContentStore::put_batch` into a `tempfile::TempDir`; assert each artifact `exists` and `verify`s. Run the whole load→compile_all→wrap pipeline twice over two independent temp stores and assert per-`CompiledTarget` `envelope_hash`/`content_hash` are identical across runs — compare per-artifact hashes, not raw `StoreManifest` bytes (`stored_at_epoch` differs by design; mirror ckc-store `cas_manifest_hash_is_stable` and `crates/ckc-retrieve/tests/persistence.rs`). Gate: `cargo test -p ckc-compile` persistence tests pass; every CompiledTarget stored/verified/content-hashed; per-artifact hashes match across two independent runs; `scripts/ci.sh` green; all workspace tests pass.
- [ ] 0.9 Verification and certificates (SPEC 13)
- [ ] 0.10 Conflict detection (SPEC 15)
- [ ] 0.11 CLI commands (SPEC 18)
- [ ] 0.12 Report and UI
- [ ] 0.13 Replay and determinism
