# CKC Roadmap

Phases contain top-level tasks; top-level tasks may contain indented subtasks.
See `.agent/prompt.md` for the session workflow that governs progression.

Top-level task annotations:
- `@<short-hash>`: the Type A commit that opened the task; the start of its
  review range. The first roadmap item uses the initial commit.
- ` reviewed`: a Type R (per-task) or Type P (Phase) review has covered the task.

A trailing `(NNNK/200K)` on a completed subtask records the session's
context-window usage at completion, produced by `python3 .agent/context_window.py`
just before commit. Type A reads the prior top-level task's annotations to
calibrate new subtask granularity.

## Phase 0: Proof-Carrying Research Kernel

- [x] 0.1 Project scaffold @a9a3321 reviewed
- [x] 0.2 CKC schema v0 (SPEC 10) @8f0ae42 reviewed
- [x] 0.3 Normal Form (SPEC 11) @642fabb reviewed
- [x] 0.4 Content-addressed store @c96a48c reviewed
- [x] 0.5 Toy fixtures (SPEC 20 Phase 0 scenarios) @8933fdc reviewed
- [x] 0.6 Terminology and e-graph @f0151f5 reviewed
- [x] 0.7 Retrieval fixture @3a6d8c4 reviewed
- [x] 0.8 Compiler targets (SPEC 14) @24b00a2 reviewed
- [x] 0.9 Verification and certificates (SPEC 13) @aca8d79 reviewed
- [x] 0.10 Conflict detection (SPEC 15) @26da1a2 reviewed
- [x] 0.11 CLI commands (SPEC 18) @8413389
  - [x] 0.11.1 ckc-cli lib scaffold + RunManifest + emit + clap dispatch: convert `crates/ckc-cli` to bin+lib. Expand `crates/ckc-cli/Cargo.toml` `[dependencies]` with `ckc-core`/`ckc-compile`/`ckc-verify`/`ckc-conflict`/`ckc-term`/`ckc-retrieve`/`ckc-store` `{ path = "../…" }` + `serde`/`serde_json`/`schemars`/`anyhow` `{ workspace = true }`, dev-dep `tempfile = { workspace = true }`. Add `src/lib.rs` declaring `pub mod emit; pub mod manifest; pub mod pipeline;` + path-qualified re-exports `ckc_compile::{CompileBundle, compile_all, portfolio_manifest, ARTIFACT_PATHS}`, `ckc_verify::{verify_all, VerificationReport, verification_manifest}`, `ckc_conflict::{detect_all, conflict_manifest}`, `ckc_core::canonical::{ContentHash, content_hash, to_canonical_bytes}`. `src/manifest.rs`: `pub struct RunManifestEntry { pub stage: String, pub artifact_kind: String, pub artifact_path: String, pub content_hash: ContentHash }` + `pub struct RunManifest { pub command: String, pub producer_version: String, pub entries: Vec<RunManifestEntry> }` (derive Clone/Debug/PartialEq/Serialize/Deserialize/JsonSchema; `producer_version`=`env!("CARGO_PKG_VERSION")`; NO timestamps — cross-run determinism per memory CAS note). `src/emit.rs`: `pub fn write_artifact(out_dir: &Path, rel_path: &str, bytes: &[u8]) -> anyhow::Result<()>` (create parent dirs under out_dir, write bytes). `src/pipeline.rs`: `pub fn load_bundle(path: &str) -> anyhow::Result<CompileBundle>` (`CompileBundle::load_toy()` for `"examples/research_kernel"`, else `anyhow::bail!`) + stub fns (`anyhow::bail!("pending")`) `run_compile(&CompileBundle,&Path)->Result<Vec<RunManifestEntry>>`, `run_verify(&CompileBundle,&Path)->Result<(VerificationReport,Vec<RunManifestEntry>)>`, `run_conflicts(&CompileBundle,&VerificationReport,&Path)->Result<Vec<RunManifestEntry>>`, `run_substrate(&CompileBundle,&Path)->Result<Vec<RunManifestEntry>>`, `run_demo(scenario:&str,replay:bool,out_dir:&Path)->Result<RunManifest>`. Rewrite `src/main.rs` as a thin clap shell: `Command` enum `Compile{bundle,out}`/`Verify{bundle,out}`/`Conflicts{bundle,out}`/`Demo{scenario,replay,out}` (default `--out runs/research`) dispatching to `pipeline::run_*`. `tests/scaffold.rs`: one `RunManifest` serialize→deserialize→equal roundtrip + one `emit::write_artifact` into a `tempfile::TempDir` asserting the file exists with exact bytes. Gate: `cargo test -p ckc-cli` passes; workspace builds; all workspace tests pass. (111K/200K)
  - [x] 0.11.2 `run_compile` + `ckc compile`: implement `pipeline::run_compile` — call `compile_all(bundle)` + `portfolio_manifest(bundle)`, write each target's `artifact_text` under `out_dir` at `ARTIFACT_PATHS[i]` via `emit::write_artifact`, return one `RunManifestEntry{ stage:"compile", artifact_kind:"compiled_target", artifact_path: ARTIFACT_PATHS[i].into(), content_hash: <portfolio_manifest entry hash> }` per target (9). `tests/compile.rs`: over `load_bundle("examples/research_kernel")` into a `TempDir`, assert 9 files written; each `out_dir/<ARTIFACT_PATHS[i]>` bytes equal the committed repo file at the same relative path (byte-identity vs `logic/{smt,asp,datalog,tla,alloy}/*` + `lean/Ckc/NormConflict.lean`); the 9 `content_hash`es equal `portfolio_manifest` hashes in order; two runs into separate tempdirs are byte-identical (determinism). Gate: `cargo test -p ckc-cli --test compile`; all workspace tests pass. (103K/200K)
  - [x] 0.11.3 `run_verify` + `ckc verify`: implement `pipeline::run_verify` — call `verify_all(bundle)`→`VerificationReport`, write each `Certificate` to `certs/certificates/<certificate_id>.json`, each `ExecutionWitness` to `certs/witnesses/<witness_id>.json` (bytes=`to_canonical_bytes`), `report.graph` to `certs/certificate_graph.json`, `report.assurance` to `certs/assurance_seed.json`, and the cvc5 proof (`include_bytes!("../../../examples/research_kernel/fixtures/cvc5_norm_conflict.proof")`) to `certs/cvc5_norm_conflict.proof` (file only — referenced via `cert_cvc5_norm_conflict.proof_artifact_hashes`, so NO separate manifest entry); build `RunManifestEntry`s (stage "verify") from `verification_manifest(bundle)` (24); return `(report, entries)`. `tests/verify.rs`: over the toy bundle into a `TempDir`, assert every `out_dir/certs/certificates/*.json`, `certs/witnesses/*.json`, `certs/certificate_graph.json`, `certs/assurance_seed.json`, `certs/cvc5_norm_conflict.proof` byte-equals its committed repo counterpart; the 24 entry hashes equal `verification_manifest` hashes; determinism over two runs. Gate: `cargo test -p ckc-cli --test verify`; all workspace tests pass. (106K/200K)
  - [x] 0.11.4 `run_conflicts` + `ckc conflicts`: implement `pipeline::run_conflicts` — call `detect_all(bundle, report)`→`ConflictReport`, write each `Conflict` to `certs/conflicts/<conflict_id>.json` and each `ArgumentGraph` to `certs/argument_graphs/<argument_graph_id>.json` (bytes=`to_canonical_bytes`), build `RunManifestEntry`s (stage "conflicts") from `conflict_manifest(bundle, report)` (4). The standalone `Command::Conflicts` handler computes `verify_all(&bundle)` then calls `run_conflicts`. `tests/conflicts.rs`: over the toy bundle (with `verify_all`) into a `TempDir`, assert the 3 `certs/conflicts/*.json` + 1 `certs/argument_graphs/*.json` (`nf-…` filenames) byte-equal committed counterparts; the 4 entry hashes equal `conflict_manifest`; determinism. Gate: `cargo test -p ckc-cli --test conflicts`; all workspace tests pass. (99K/200K)
  - [x] 0.11.5 `run_substrate` (RDF/SHACL + retrieval emission): implement `pipeline::run_substrate` — (a) `TerminologyGraph::load_from_json(<concepts.json>)` then write `ckc_term::rdf::export_skos_turtle(&g)` to `out_dir/terminology.ttl`; (b) `ckc_term::shacl::validate_rules(&bundle.rules)`→`to_canonical_bytes` to `out_dir/shacl_report.json`; (c) `SparseIndex::build_from_spans(&bundle.spans)`, run the committed `queries.json` queries (mirror however `retrieval_results.json` is produced — inspect `crates/ckc-retrieve/tests/persistence.rs`), assemble `Vec<RetrievalResult>`, write `to_canonical_bytes` to `out_dir/retrieval/retrieval_results.json`; return `RunManifestEntry`s (stage "substrate"; kinds "rdf_export"/"shacl_report"/"retrieval_result"; content_hash via in-memory `content_hash`). `tests/substrate.rs`: `terminology.ttl` and `shacl_report.json` byte-equal committed `examples/research_kernel/fixtures/{terminology.ttl,shacl_report.json}`; for retrieval use STRUCTURAL comparison not byte-identity (serde_json f64 parse/format asymmetry — memory 2026-05-29): deserialize the written file and assert query ids + per-hit `span_id` order + `index_fingerprint` match committed `retrieval_results.json`, and equal in-memory `content_hash` across two runs. Gate: `cargo test -p ckc-cli --test substrate`; all workspace tests pass. (163K/200K)
  - [x] 0.11.6 `run_demo` orchestration + `--replay` + run manifest: implement `pipeline::run_demo` — `let bundle = load_bundle("examples/research_kernel")?`; concatenate entries from `run_compile` + `run_verify` (keep its `report`) + `run_conflicts(&bundle,&report,out_dir)` + `run_substrate` (all under `out_dir`); assemble `RunManifest{ command:"ckc demo research-kernel --out <dir>", producer_version, entries }`; write `to_canonical_bytes(&manifest)` to `out_dir/run_manifest.json`; when `replay`, re-run the whole pipeline a second time and `anyhow::bail!` if `content_hash(&manifest2) != content_hash(&manifest1)`. Wire `Command::Demo` → `run_demo`. `tests/demo.rs`: run `run_demo("research-kernel", true, tmp)`; assert `out_dir/run_manifest.json` exists and `manifest.entries.len()` equals the sum of the four stage manifests; every compiled/cert/conflict file present under `out_dir`; two `run_demo` runs into separate tempdirs yield equal `content_hash(&RunManifest)` (determinism); plus one end-to-end test via `std::process::Command::new(env!("CARGO_BIN_EXE_ckc")).args(["demo","research-kernel","--replay","--out",<tmp>])` asserting success + `run_manifest.json` written (`runs/*/` already gitignored). Standalone `ckc replay <manifest>`, the committed `runs/research` replay manifest, and CAS-store-of-run are 0.13. Gate: `cargo test -p ckc-cli --test demo`; `scripts/ci.sh` green; all workspace tests pass. (111K/200K)
- [ ] 0.12 Report and UI
- [ ] 0.13 Replay and determinism
