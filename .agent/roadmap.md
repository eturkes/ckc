# CKC Roadmap

Phases contain top-level tasks; top-level tasks may contain indented subtasks.
See `.agent/prompt.md` for the session workflow that governs progression.

A trailing `(NNNK/200K)` on a completed subtask records the session's
context-window usage at completion, produced by `python3 .agent/context_window.py`
just before commit. Type A reads the prior top-level task's annotations to
calibrate new subtask granularity.

## Phase 0: Proof-Carrying Research Kernel

- [x] 0.1 Project scaffold
- [x] 0.2 CKC schema v0 (SPEC 10)
- [x] 0.3 Normal Form (SPEC 11)
- [x] 0.4 Content-addressed store
- [x] 0.5 Toy fixtures (SPEC 20 Phase 0 scenarios)
- [x] 0.6 Terminology and e-graph
- [x] 0.7 Retrieval fixture
  - [x] 0.7.1 Retrieval types and ArtifactKind extension: populate ckc-retrieve with core retrieval types. Define `AnalyzerConfig` (name, dictionary, mode), `RetrievalQuery` (query_id, query_text, language, analyzer_config), `RetrievalHit` (span_id, score, rank), `RetrievalResult` (query, hits, index_fingerprint, corpus_hash), and `QrelJudgment` (query_id, span_id, relevance grade). Add `ArtifactKind::RetrievalResult` to ckc-core/envelope.rs. Derive Serialize/Deserialize/JsonSchema for all types. Update ArtifactKind golden schema tests. Add ckc-core as ckc-retrieve dependency. Gate: `cargo test -p ckc-retrieve` passes with round-trip serde tests for every type; ArtifactKind schema golden updated; all workspace tests pass.
  - [x] 0.7.2 Tantivy/Lindera Japanese sparse index: add `tantivy` and `lindera-tantivy` (ipadic feature) as workspace dependencies. Implement `SparseIndex` in ckc-retrieve: build from `Vec<SourceSpan>` into a Tantivy in-memory index with fields span_id (stored), search_text (indexed+stored, Lindera IPAdic tokenizer), nfkc_text (indexed, Lindera IPAdic tokenizer), doc_id (stored), section_path (stored). BM25 ranked search: accept query text, return top-k `Vec<RetrievalHit>`. Index fingerprint: SHA-256 of sorted (span_id, content_hash) pairs, deterministic regardless of insertion order. Unit tests: index all 17 toy spans; query "敗血症 βラクタム 投与" returns span_rec_sepsis_bl in top 3; query "体温" returns vitals table-cell spans; query "アナフィラキシー 禁忌" returns span_contra_bl_allergy; index fingerprint identical across two independent builds from same spans. Gate: `cargo test -p ckc-retrieve` passes; Lindera IPAdic morphological analysis produces expected token counts; BM25 returns relevant spans for JA queries; fingerprint is deterministic.
  - [x] 0.7.3 Query and qrel fixtures: author `examples/research_kernel/fixtures/queries.json` (`Vec<RetrievalQuery>`, 6–8 JA queries over Phase 0 scenarios — q_sepsis_bl "敗血症 βラクタム 投与", q_contra_allergy "アナフィラキシー 禁忌", q_allergy_history, q_vitals_temp "体温", q_provenance, q_term_variants; each language=Ja, analyzer_config {name lindera_ipadic, dictionary ipadic, mode normal}) and `examples/research_kernel/fixtures/qrels.json` (`Vec<QrelJudgment>`, relevance grades 0–3 linking each query to its target span(s): q_sepsis_bl→span_rec_sepsis_bl, q_contra_allergy→span_contra_bl_allergy, q_allergy_history→span_allergy_history, q_vitals_temp→span_cell_* temperature cells, q_provenance→span_provenance_meta, q_term_variants→span_term_bl_greek+span_term_bl_katakana). Add `crates/ckc-retrieve/tests/fixtures.rs` that `include_str!` both fixtures plus toy `spans.json`, deserializes each, and asserts every qrel query_id resolves to a query, every qrel span_id exists in spans, and query count is in [6,8]. Gate: `cargo test -p ckc-retrieve` passes; both fixtures deserialize; all qrel ids resolve; all workspace tests pass. (91K/200K)
  - [x] 0.7.4 Evaluation metrics: add `crates/ckc-retrieve/src/metrics.rs` (re-exported from lib.rs) with pure functions over a rank-ordered `&[RetrievalHit]` — `recall_at_k(hits, relevant: &HashSet<SpanId>, k)`, `reciprocal_rank(hits, relevant: &HashSet<SpanId>)`, `ndcg_at_k(hits, graded: &HashMap<SpanId,u32>, k)` (DCG = Σ relᵢ/log2(rankᵢ+1) over top-k, divided by ideal DCG; return 0.0 when ideal DCG is 0). Unit tests use tiny synthetic hit lists with inline relevance and assert each metric against a hand-computed constant written in a comment; cover empty hits, zero relevant, all relevant, and k larger than hits. Gate: `cargo test -p ckc-retrieve` metrics tests pass; computed values equal the documented hand-calculated numbers; all workspace tests pass. (90K/200K)
  - [x] 0.7.5 Retrieval pipeline and evaluation (in-memory): add `crates/ckc-retrieve/tests/pipeline.rs` that `include_str!` toy `spans.json` + `queries.json` + `qrels.json`, builds `SparseIndex::build_from_spans`, runs `search` (top_k=5) per query, assembles a `RetrievalResult` per query (index_fingerprint from `compute_index_fingerprint`; corpus_hash from an existing corpus-hash helper if present, else `ckc_core::canonical::content_hash` over the span vec sorted by span_id — document the choice), groups qrels by query_id, and computes Recall@{1,3,5}, MRR, nDCG@5 via 0.7.4. Assert per-query expected top spans (q_sepsis_bl: span_rec_sepsis_bl in top-3; q_contra_allergy: span_contra_bl_allergy in top-3; q_vitals_temp: a vitals cell span in top-5) and aggregate MRR > 0.5. Gate: pipeline test passes; expected top spans retrieved per query; MRR > 0.5; all workspace tests pass. (96K/200K)
  - [x] 0.7.6 RetrievalResult CAS persistence and determinism: add ckc-store + tempfile as ckc-retrieve dev-dependencies; in `crates/ckc-retrieve/tests/persistence.rs` rebuild the 0.7.5 pipeline, wrap each `RetrievalResult` via `ArtifactEnvelope::wrap(ArtifactKind::RetrievalResult, &r, meta with stage "retrieve")`, `ContentStore::put_batch` into a `tempfile::TempDir` store, and assert each artifact `exists` and `verify`s. Run the whole build→search→wrap pipeline twice over two independent temp stores and assert per-result `envelope_hash`/`content_hash` are identical across runs (compare per-artifact hashes, not raw `StoreManifest` bytes — `stored_at_epoch` differs by design; mirror ckc-store `cas_manifest_hash_is_stable`). Serialize the produced results to `examples/research_kernel/fixtures/retrieval_results.json` and commit it. Gate: every RetrievalResult is stored, verified, and content-hashed; per-result hashes match across two independent runs; result fixture committed; all workspace tests pass. (160K/200K)
- [ ] 0.8 Compiler targets (SPEC 14)
- [ ] 0.9 Verification and certificates (SPEC 13)
- [ ] 0.10 Conflict detection (SPEC 15)
- [ ] 0.11 CLI commands (SPEC 18)
- [ ] 0.12 Report and UI
- [ ] 0.13 Replay and determinism
