# CKC Roadmap

Phases contain top-level tasks; top-level tasks may contain indented subtasks.
See `.agent/prompt.md` for the session workflow that governs progression.

Top-level task annotations:
- `@<short-hash>`: the Type A commit that opened the task; the start of its
  review range. The first roadmap item uses the initial commit.
- ` reviewed`: a Type R (per-task) or Type P (Phase) review has covered the task.

A trailing `(NNNK/200K)` on a completed subtask records the session's
context-window usage at completion, produced by `python3 .agent/context_window.py`
just before commit. Type A reads the prior top-level task's annotations to
calibrate new subtask granularity.

## Phase 0: Proof-Carrying Research Kernel

- [x] 0.1 Project scaffold @a9a3321 reviewed
- [x] 0.2 CKC schema v0 (SPEC 10) @8f0ae42 reviewed
- [x] 0.3 Normal Form (SPEC 11) @642fabb reviewed
- [x] 0.4 Content-addressed store @c96a48c reviewed
- [x] 0.5 Toy fixtures (SPEC 20 Phase 0 scenarios) @8933fdc reviewed
- [x] 0.6 Terminology and e-graph @f0151f5 reviewed
- [x] 0.7 Retrieval fixture @3a6d8c4 reviewed
- [x] 0.8 Compiler targets (SPEC 14) @24b00a2 reviewed
- [x] 0.9 Verification and certificates (SPEC 13) @aca8d79 reviewed
- [x] 0.10 Conflict detection (SPEC 15) @26da1a2 reviewed
  - [x] 0.10.1 ckc-conflict crate scaffold + aggregate types: create `crates/ckc-conflict/` and add `crates/ckc-conflict` to root `Cargo.toml` `[workspace] members`; Cargo.toml mirrors `crates/ckc-verify/Cargo.toml` — deps `ckc-core`/`ckc-compile`/`ckc-verify` `{ path = "../…" }` + serde/serde_json/schemars/anyhow `{ workspace = true }`, dev-deps `ckc-store = { path = "../ckc-store" }` + `tempfile = { workspace = true }`. `src/lib.rs` declares `pub mod link; pub mod repair; pub mod argument; pub mod detect;` (create each as an empty file with a doc comment for later subtasks) and defines `pub struct ConflictReport { pub conflicts: Vec<Conflict>, pub argument_graphs: Vec<ArgumentGraph>, pub diagnostics: Vec<CompileDiagnostic> }`, `pub struct ConflictManifestEntry { pub artifact_kind: String, pub artifact_path: String, pub content_hash: ContentHash }`, transparent newtype `pub struct ConflictManifest(pub Vec<ConflictManifestEntry>)` — all derive Clone/Debug/PartialEq/Serialize/Deserialize/JsonSchema, no Normalize (composition of already-normalized artifacts; mirror `ckc_verify::VerificationReport`). Path-qualified re-exports: `ckc_core::verify::{Conflict, ArgumentGraph}`, `ckc_core::compile::CompileDiagnostic`, `ckc_core::canonical::{ContentHash, content_hash}`, `ckc_compile::CompileBundle`, `ckc_verify::{verify_all, VerificationReport}`. `tests/report.rs`: one roundtrip assertion over an empty `ConflictReport` (serialize→deserialize→equal) and one `content_hash` stability check. Gate: `cargo test -p ckc-conflict` passes; crate builds in the workspace; all workspace tests pass. (89K/200K)
  - [x] 0.10.2 Evidence-linkage resolver (`src/link.rs`): the layer that replaces the placeholder cross-references in `conflicts.json` (`witness:"witness_norm_conflict"`, `minimal_artifact_set:["sha256:a00…"]`) with the real `ckc_verify::verify_all` artifacts. Add `pub fn witness_for_cert(report: &VerificationReport, cert_id: &str) -> Option<WitnessId>` returning the `nf-…` `witness_id` of the `ExecutionWitness` whose `certificate_ids` contains `cert_id`, and `pub fn minimal_artifact_set(report: &VerificationReport, cert_ids: &[&str]) -> Vec<ContentHash>` returning the sorted+deduped `ckc_core::canonical::content_hash` of each `Certificate` whose `certificate_id` is in `cert_ids`. Per-class cert keys (the deterministic `cert_<solver>_<stem>` ids from 0.9 `certificate_for`): norm_contradiction → `["cert_z3_norm_conflict","cert_cvc5_norm_conflict","cert_lean_norm_conflict","cert_clingo_defeasible"]`, primary witness cert `cert_z3_norm_conflict`; decision_table_overlap → `["cert_z3_decision_table"]`; temporal_violation → `["cert_clingo_event_calculus"]`. `tests/link.rs`: over `verify_all(&CompileBundle::load_toy())` assert `witness_for_cert(&report,"cert_z3_norm_conflict")` is `Some` and starts with `nf-` (not `witness_norm_conflict`); `minimal_artifact_set(&report, &[the 4 norm keys])` returns ≥3 distinct `sha256:`-prefixed hashes; determinism over two calls. Gate: `cargo test -p ckc-conflict --test link` passes; all workspace tests pass. (116K/200K)
  - [x] 0.10.3 Repair-candidate generators (`src/repair.rs`): SPEC 15 minimal repairs framed as source-revision review prompts, tied to the existing MaxSMT target (`logic/smt/repair_maxsmt.smt2` → z3 `sat`, recorded `objective: Some(1)` from 0.9.2). Add `pub fn repair_candidates(conflict_type: &str, oracle: &Conflict) -> Vec<serde_json::Value>` returning, per class, the candidate set whose `type`/fields match the authored `bundle.conflicts` `repair_candidates`: norm_contradiction → `add_priority`(superior `rule_bl_anaphylaxis_contra`) + `add_exception`(rule `rule_sepsis_bl_recommend`, exception `beta_lactam_anaphylaxis`); decision_table_overlap → `refine_condition`(row `row_temp_high`, `38.0 <= temperature < 38.5`) + `change_hit_policy`(unique→priority); temporal_violation → `add_temporal_guard`(action `administer_drug`, guard `NOT holds(allergy_known, t)`); reuse the matching oracle entry's `rationale` strings verbatim so canonical bytes agree. Add `pub fn minimal_repair_objective(report: &VerificationReport) -> Option<i64>` reading the recorded `repair_maxsmt` outcome's `objective`. `tests/repair.rs`: assert norm yields 2 candidates `add_priority`+`add_exception`, dt yields `refine_condition`+`change_hit_policy`, temporal yields `add_temporal_guard`; each generated set equals the matching `conflicts.json` `repair_candidates` after `to_canonical_bytes`; `minimal_repair_objective(&verify_all(&CompileBundle::load_toy()))` is `Some(1)`. Gate: `cargo test -p ckc-conflict --test repair` passes; all workspace tests pass. (121K/200K)
  - [x] 0.10.4 Argument-graph assembly (`src/argument.rs`): SPEC 13.3 / 15.1 #13 Dung argumentation over the defeasible rule pair. Add `pub fn build_argument_graph(bundle: &CompileBundle) -> ArgumentGraph` that loads the authored content oracle via `include_str!("../../../examples/research_kernel/fixtures/argument_graphs.json")` (single `ag_sepsis_bl_conflict` graph; `CompileBundle` does not carry argument graphs), validates every `arguments[].source_rule` resolves in `bundle.rules` (`arg_recommend_bl→rule_sepsis_bl_recommend`, `arg_contraindicate_bl→rule_bl_anaphylaxis_contra`) and `source_span_ids ⊆ bundle.spans`, then runs `ckc_core::nf::normalize_all` (→ `nf-…` `argument_graph_id`; sorts the six graph arrays + spans). `tests/argument.rs`: assert the graph has the 3 arguments, one `rebut` attack `arg_contraindicate_bl→arg_recommend_bl`, one `unresolved` defeat edge, and a `grounded` extension rejecting `arg_recommend_bl`; `argument_graph_id` starts with `nf-`; every `source_rule` is a bundle rule id; determinism over two calls (equal `content_hash`). Gate: `cargo test -p ckc-conflict --test argument` passes; all workspace tests pass. (101K/200K)
  - [x] 0.10.5 Norm-contradiction detector (`src/detect.rs::detect_norm_contradiction`): SPEC 15.1 #1/#2. `pub fn detect_norm_contradiction(bundle: &CompileBundle, report: &VerificationReport) -> Vec<Conflict>` scans `bundle.rules` for a defeasible for/against pair sharing one `norm.action.target_concept` (`rule_sepsis_bl_recommend` direction `for` vs `rule_bl_anaphylaxis_contra` direction `against`, both `concept_beta_lactam`) and assembles one `Conflict`: computed `conflict_type="norm_contradiction"`, `classification=ConflictClassification::TrueConflict`, `severity=Severity::High`; the curated `source_spans`, `confidence`, `normalized_view`, `solver_evidence`, `human_review_question_{ja,en}` reused from the authored `bundle.conflicts` entry whose `conflict_type=="norm_contradiction"` (the content oracle); `repair_candidates = repair::repair_candidates("norm_contradiction", oracle)`; `witness = link::witness_for_cert(report,"cert_z3_norm_conflict")`; `minimal_artifact_set = link::minimal_artifact_set(report, &["cert_z3_norm_conflict","cert_cvc5_norm_conflict","cert_lean_norm_conflict","cert_clingo_defeasible"])`; `argument_graph_id = Some(argument::build_argument_graph(bundle).argument_graph_id)`; run `normalize_all` (→ `nf-…` `conflict_id`). `tests/detect_norm.rs`: assert exactly one conflict; the detected for/against pair is `{rule_sepsis_bl_recommend, rule_bl_anaphylaxis_contra}` (detection-agreement); `conflict_type`/`classification`/`severity` as above; `conflict_id` and `argument_graph_id` start with `nf-`; `witness` is `Some(nf-…)` (not `witness_norm_conflict`); `minimal_artifact_set` all `sha256:`; determinism. Gate: `cargo test -p ckc-conflict --test detect_norm` passes; all workspace tests pass. (133K/200K)
  - [x] 0.10.6 Decision-table-defect detector (`src/detect.rs::detect_decision_table_defects`): SPEC 15.1 #14 (overlap + gap + hit-policy). `pub fn detect_decision_table_defects(bundle: &CompileBundle, report: &VerificationReport) -> Vec<Conflict>` scans `bundle.decision_tables` for a `HitPolicy::Unique` table with two rows whose numeric conditions co-fire (`row_temp_high` `temperature>=38.0` and `row_temp_very_high` `temperature>=38.5` both fire at `>=38.5`) plus a gap point matching no row (`(37.5,85,95)`); assemble one `Conflict`: computed `conflict_type="decision_table_overlap"`, `classification=TrueConflict`, `severity=Severity::Medium`; curated `source_spans`/`confidence`/`normalized_view`/`solver_evidence`/review questions reused from the authored `decision_table_overlap` `bundle.conflicts` entry; `repair_candidates = repair::repair_candidates("decision_table_overlap", oracle)`; `witness = link::witness_for_cert(report,"cert_z3_decision_table")`; `minimal_artifact_set = link::minimal_artifact_set(report,&["cert_z3_decision_table"])`; `argument_graph_id = None`; `normalize_all`. `tests/detect_dt.rs`: assert one conflict; the detected overlapping unique-policy row pair is `{row_temp_high,row_temp_very_high}` and a gap point exists (detection-agreement); type/classification/severity; `conflict_id` starts with `nf-`; determinism. Gate: `cargo test -p ckc-conflict --test detect_dt` passes; all workspace tests pass. (123K/200K)
  - [x] 0.10.7 Temporal-violation detector (`src/detect.rs::detect_temporal_violation`): SPEC 15.1 #6/#7 Event-Calculus persistence violation. `pub fn detect_temporal_violation(bundle: &CompileBundle, report: &VerificationReport) -> Vec<Conflict>` reads the single `bundle.event_narratives` entry, confirms its `holds_queries` asserts `allergy_known` holds at the `administer_drug` time (t=10) with no terminating event, cross-checks the recorded EC witness (the one whose `certificate_ids` ∋ `cert_clingo_event_calculus`) carries `violated_constraints` containing `conflict_ec_allergy_persistence`, and assembles one `Conflict`: computed `conflict_type="temporal_violation"`, `classification=TrueConflict`, `severity=Severity::High`; curated `source_spans`/`confidence`/`normalized_view`/`solver_evidence`/review questions reused from the authored `temporal_violation` entry (assert the narrative's `source_span_ids ⊆ source_spans`); `repair_candidates = repair::repair_candidates("temporal_violation", oracle)`; `witness = link::witness_for_cert(report,"cert_clingo_event_calculus")`; `minimal_artifact_set = link::minimal_artifact_set(report,&["cert_clingo_event_calculus"])`; `argument_graph_id = Some(argument::build_argument_graph(bundle).argument_graph_id)`; `normalize_all`. `tests/detect_temporal.rs`: assert one conflict; the EC `violated_constraints` evidence was found (detection-agreement); type/classification/severity; `conflict_id` starts with `nf-`; `witness` is `Some(nf-…)`; determinism. Gate: `cargo test -p ckc-conflict --test detect_temporal` passes; all workspace tests pass. (132K/200K)
  - [x] 0.10.8 `detect_all` orchestration (`src/lib.rs`): `pub fn detect_all(bundle: &CompileBundle, report: &VerificationReport) -> ConflictReport` composing 0.10.4–0.10.7: `conflicts` = `detect::detect_norm_contradiction` ++ `detect::detect_decision_table_defects` ++ `detect::detect_temporal_violation` (3 total), `argument_graphs` = `vec![argument::build_argument_graph(bundle)]` (1), `diagnostics` = `report.disagreements.clone()` (backend-disagreement pass-through, SPEC 15.1 #16; 0 on the real oracle — the synthetic-divergence branch is already covered in `ckc-verify` portfolio, do not re-test). Add `pub fn conflict_manifest(bundle: &CompileBundle, report: &VerificationReport) -> ConflictManifest` pairing each conflict (`artifact_kind:"conflict"`, `artifact_path: format!("certs/conflicts/{}.json", conflict_id)`) then each argument graph (`artifact_kind:"argument_graph"`, `certs/argument_graphs/<id>.json`) with its `content_hash`, in that deterministic order. `tests/detect_all.rs`: assert counts (3 conflicts, 1 argument_graph, 0 diagnostics); every `conflict_id`/`argument_graph_id` starts with `nf-`; two runs of `detect_all(&CompileBundle::load_toy(), &verify_all(&CompileBundle::load_toy()))` produce equal per-artifact `content_hash` vectors (determinism). Gate: `cargo test -p ckc-conflict --test detect_all` passes; deterministic; all workspace tests pass. (97K/200K)
  - [x] 0.10.9 Committed `certs/conflicts/` + `certs/argument_graphs/` files + byte-identical regeneration: add an `#[ignore = "regenerate-only"]` writer test in `tests/detect_all.rs` (mirror `crates/ckc-verify/tests/verify_all.rs`) writing each `detect_all` artifact via `to_canonical_bytes` to `certs/conflicts/<conflict_id>.json` (×3) and `certs/argument_graphs/<argument_graph_id>.json` (×1); commit those files. Add a non-ignored gate test asserting each committed file's bytes equal the corresponding live `detect_all` artifact. Gate: `cargo test -p ckc-conflict --test detect_all` file-equality test passes; committed `certs/conflicts/*` + `certs/argument_graphs/*` regenerate byte-identically; all workspace tests pass. (89K/200K)
  - [x] 0.10.10 Conflict-manifest golden: `tests/manifest.rs` copies the `golden_suite!` harness + path helpers + `write_type`/`#[ignore] regenerate` from `crates/ckc-verify/tests/manifest.rs` and golden-locks `conflict_manifest(&CompileBundle::load_toy(), &verify_all(&CompileBundle::load_toy()))` into `schemas/golden/conflict_manifest.json` + `schemas/conflict_manifest.schema.json`; this one compact golden byte-locks every detected conflict + argument-graph artifact through its `content_hash`. Add an integrity test asserting each manifest entry's `artifact_path` resolves to a committed `certs/` file (mirror `manifest_paths_resolve_to_committed_files`). Gate: `cargo test -p ckc-conflict --test manifest` passes; golden matches; all workspace tests pass. (89K/200K)
  - [x] 0.10.11 CAS persistence + cross-run determinism: in `crates/ckc-conflict/tests/persistence.rs` (mirror `crates/ckc-verify/tests/persistence.rs`) run `detect_all(&CompileBundle::load_toy(), &verify_all(&CompileBundle::load_toy()))`, wrap each `Conflict` (`ArtifactKind::Conflict`, `semantic_profiles: vec![SemanticProfile::Para]`) and `ArgumentGraph` (`ArtifactKind::ArgumentGraph`, `vec![SemanticProfile::Defeasible]`) via `ArtifactEnvelope::wrap` (meta stage `"conflict"`, placeholder all-zeros `content_hash`, `replay_command: Some("ckc demo research-kernel --replay --out runs/research")`) and `ContentStore::put_batch` into a `tempfile::TempDir`; assert each `exists`+`verify`s and each envelope `meta.content_hash == content_hash(artifact)`. Run the load→verify_all→detect_all→wrap pipeline twice over two independent temp stores and assert per-artifact `envelope_hash`/inner `content_hash`/store keys match across runs (compare per-artifact hashes, not raw `StoreManifest` bytes — `stored_at_epoch` differs by design). Gate: `cargo test -p ckc-conflict --test persistence` passes; every conflict + argument-graph artifact stored/verified/content-hashed; per-artifact hashes match across two runs; `scripts/ci.sh` green; all workspace tests pass. (88K/200K)
- [ ] 0.11 CLI commands (SPEC 18)
- [ ] 0.12 Report and UI
- [ ] 0.13 Replay and determinism
