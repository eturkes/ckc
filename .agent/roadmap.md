# CKC Roadmap

Phases contain top-level tasks; top-level tasks may contain indented subtasks.
See `.agent/prompt.md` for the session workflow that governs progression.

## Phase 0: Proof-Carrying Research Kernel

- [x] 0.1 Project scaffold
- [x] 0.2 CKC schema v0 (SPEC 10)
- [x] 0.3 Normal Form (SPEC 11)
- [x] 0.4 Content-addressed store
- [x] 0.5 Toy fixtures (SPEC 20 Phase 0 scenarios)
- [x] 0.6 Terminology and e-graph
  - [x] 0.6.1 TerminologyGraph and concept indexing (ckc-term): populate the empty ckc-term crate. Define `TerminologyGraph` holding `Concept` and `TerminologyBinding` entries (reusing types from ckc-core) with in-memory indexes by concept_id, egraph_class_id, system+code, and label substring (ja/en). Operations: `insert`, `get_by_id`, `get_by_egraph_class`, `find_by_system_code`, `search_by_label`, `load_from_json` (deserialize a Vec<Concept> fixture). Add ckc-core as a dependency. Gate: `cargo test -p ckc-term` passes; all 11 toy concepts from 0.5.3 load and query correctly; variant concepts (5 beta-lactam entries) return together under `get_by_egraph_class("eclass_beta_lactam")`; search_by_label finds concepts by partial JA/EN match.
  - [x] 0.6.2 E-graph equivalence engine: add `egg` workspace dependency (prefer egg for programmatic Rust API; egglog as alternative if declarative rules prove clearer). Define a CKC term language for egg with node types: ConceptRef(ConceptId), Label(String), EgraphClass(String). Rewrite rules: (a) concepts sharing egraph_class_id are equivalent, (b) Japanese variant patterns (βラクタム ↔ ベータラクタム ↔ β-ラクタム via label normalization), (c) brand-to-class subsumption (セフトリアキソン narrower-than βラクタム class). Cost function: prefer the canonical concept (shortest concept_id or designated primary). `TermEquivalence` engine: load from TerminologyGraph, run bounded saturation, extract canonical representative per e-class, emit `EgraphArtifact` (JSON: class_ids, canonical representatives, rewrite count, saturation stats, iterations). Store EgraphArtifact through CAS. Gate: `cargo test -p ckc-term` passes; all 5 beta-lactam variant concepts converge to one e-class; EgraphArtifact is deterministic and content-hashed; saturation terminates within bounded iterations.
  - [x] 0.6.3 NF pass 8 real implementation: replace the identity stub in `nf.rs` pass 8 with e-graph terminology normalization. `NfContext` gains an optional `&TermEquivalence` reference. When normalizing any CKC object containing concept_id references, terminology labels, or action target_concept fields, resolve through the e-graph canonical representative. Concept-bearing types affected: Rule (context/antecedent/consequent Value trees containing concept refs), ClinicalClaim (indirectly via rules), Action (target_concept), Norm (action), DecisionTable (cell values referencing concepts). Update existing NF golden fixtures to reflect terminology-normalized output. Existing pass 8 identity tests become convergence tests. Property test: two Rules identical except one references concept_bl_variant_katakana and the other concept_bl_variant_hyphenated produce identical NF canonical bytes. Gate: all existing `cargo test -p ckc-core` NF tests pass with updated goldens; new variant-convergence property test passes; pass 8 records rewrites in the rewrite log.
  - [x] 0.6.4 RDF/SKOS/Turtle export and SHACL validation: add `oxrdf` + `oxttl` workspace dependencies for RDF/Turtle serialization. Implement `export_skos_turtle(&TerminologyGraph) -> String` in ckc-term: each Concept becomes a skos:Concept with skos:prefLabel (label_ja), skos:altLabel (label_en), terminology bindings as skos:exactMatch/broadMatch/narrowMatch/relatedMatch by BindingStatus, egraph_class_id as a shared skos:ConceptScheme or owl:equivalentClass grouping, and source provenance triples. Implement minimal SHACL-subset validator in ckc-term (NodeShape + sh:minCount constraint, sufficient for Phase 0): define shapes requiring Rule to have ≥1 source_span_id and non-empty provenance. Validate toy fixtures including the provenance-incomplete rule from 0.5.2 (scenario 6). Emit structured `ShaclReport` (violations with focus node, path, message, severity). Gate: RDF export round-trips through oxttl parse; SHACL catches provenance-incomplete rule; passing rules validate clean; ShaclReport is serializable and content-hashable; committed as fixture.
  - [x] 0.6.5 Alignment coherence, integration, and committed artifacts: implement `check_alignment_coherence(&TerminologyGraph, &TermEquivalence)` in ckc-term: detect concepts sharing an egraph_class_id but having terminology bindings in the same system with different codes and no declared mapping relation between them; emit `AlignmentIncoherence` diagnostic with concept pair, conflicting bindings, and repair suggestion. Plant one incoherent binding in a test-only fixture to verify detection. Full integration test of 0.6 pipeline: load 0.5.x fixtures → build TerminologyGraph → run e-graph saturation → NF pass 8 on all Rules/Claims → RDF/SKOS export → SHACL validate → coherence check → store all artifacts (EgraphArtifact, RDF export, ShaclReport, AlignmentIncoherence diagnostics) via CAS → verify deterministic manifest. Gate: planted incoherent alignment detected; clean alignments pass; integration test passes end-to-end; all artifacts committed; manifest hash is deterministic across reruns.
- [ ] 0.7 Retrieval fixture
- [ ] 0.8 Compiler targets (SPEC 14)
- [ ] 0.9 Verification and certificates (SPEC 13)
- [ ] 0.10 Conflict detection (SPEC 15)
- [ ] 0.11 CLI commands (SPEC 18)
- [ ] 0.12 Report and UI
- [ ] 0.13 Replay and determinism
