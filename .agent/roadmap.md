# CKC Roadmap

Phases contain top-level tasks; top-level tasks may contain indented subtasks.
See `.agent/prompt.md` for the session workflow that governs progression.

Top-level task annotations:
- `@<short-hash>`: the Type A commit that opened the task; the start of its
  review range. The first roadmap item uses the initial commit.
- ` reviewed`: a Type R (per-task) or Type P (Phase) review has covered the task.

A trailing `(NNNK/200K)` on a completed subtask records the session's
context-window usage at completion, produced by `python3 .agent/context_window.py`
just before commit. Type A reads the prior top-level task's annotations to
calibrate new subtask granularity.

## Phase 0: Proof-Carrying Research Kernel

- [x] 0.1 Project scaffold @a9a3321
- [x] 0.2 CKC schema v0 (SPEC 10) @8f0ae42
- [x] 0.3 Normal Form (SPEC 11) @642fabb
- [x] 0.4 Content-addressed store @c96a48c
- [x] 0.5 Toy fixtures (SPEC 20 Phase 0 scenarios) @8933fdc
- [x] 0.6 Terminology and e-graph @f0151f5
- [x] 0.7 Retrieval fixture @3a6d8c4
- [x] 0.8 Compiler targets (SPEC 14) @24b00a2 reviewed
- [ ] 0.9 Verification and certificates (SPEC 13) @aca8d79
  - [x] 0.9.1 ckc-verify crate scaffold + verdict types: create `crates/ckc-verify/` and add it to root `Cargo.toml` `[workspace] members`; Cargo.toml deps `ckc-core`/`ckc-compile`/`ckc-term` `{ path = "../…" }` + serde/serde_json/schemars/anyhow `{ workspace = true }`, dev-deps `ckc-store = { path = "../ckc-store" }` + `tempfile = { workspace = true }`. Add `crates/ckc-verify/src/verdict.rs` (`pub mod verdict;`) with: enum `SolverId` (`Z3, Cvc5, Clingo, Souffle, Lean, TlaSany, Alloy, Shacl`; `#[serde(rename_all="snake_case")]`); enum `VerdictStatus` (`Unsat, Sat, Satisfiable, NoCounterexample, EmptyRelation, SemanticCheckPassed, KernelChecked, ViolationsFound`; snake_case); struct `VerifierOutcome { target_language: TargetLanguage, artifact_path: String, solver: SolverId, status: VerdictStatus, salient_atoms: Vec<String>, objective: Option<i64>, proof_present: bool, certificate_class: CertificateClass }`; transparent newtype `RecordedOutcomes(pub Vec<VerifierOutcome>)`. Derive Clone/Debug/PartialEq/Serialize/Deserialize/JsonSchema; add no Normalize impl (outcomes are authored deterministically as a recorded oracle). `src/lib.rs`: `pub mod verdict;` + path-qualified re-exports of the verdict types and `ckc_core::canonical::{ContentHash, content_hash}` (ckc-core has no flattening re-exports — path-qualify e.g. `ckc_core::enums::{TargetLanguage, CertificateClass}`). `tests/verdict.rs`: one roundtrip assertion (build a `VerifierOutcome`, serialize→deserialize→equal) and one `content_hash` stability check. Gate: `cargo test -p ckc-verify` passes; crate builds in the workspace; all workspace tests pass. (95K/200K)
  - [x] 0.9.2 Recorded solver-outcome oracle fixture + cvc5 proof fixture + loader + golden: author `examples/research_kernel/fixtures/verifier_outcomes.json` as a `RecordedOutcomes` array of 11 entries — the 9 `compile_all` targets (in `ARTIFACT_PATHS` order) plus a cvc5 entry plus a SHACL entry — recording the verdicts confirmed against the installed solvers: z3 `logic/smt/norm_conflict.smt2`→`unsat`(C4-Executable); z3 `logic/smt/decision_table.smt2`→`sat`(C4); z3 `logic/smt/repair_maxsmt.smt2`→`sat`, `objective: Some(1)`(C4); clingo `logic/asp/defeasible.lp`→`satisfiable`, salient_atoms `["defeated(rule_sepsis_bl_recommend)","in(arg_rule_bl_anaphylaxis_contra)","defeated_arg(arg_rule_sepsis_bl_recommend)"]`(C4); clingo `logic/asp/event_calculus.lp`→`satisfiable`, salient_atoms `["holdsAt(allergy_known,10)","violation(conflict_ec_allergy_persistence)"]`(C4); souffle `logic/datalog/priority.dl`→`empty_relation`(C4); lean `lean/Ckc/NormConflict.lean`→`kernel_checked`(C7-Kernel); tla `logic/tla/Conflict.tla`→`semantic_check_passed`(C4); alloy `logic/alloy/Priority.als`→`no_counterexample`(C4); cvc5 (`SolverId::Cvc5`) `logic/smt/norm_conflict.smt2`→`unsat`, `proof_present=true`(C6-ProofObject); shacl (`SolverId::Shacl`) `examples/research_kernel/fixtures/shacl_report.json`→`violations_found`, salient_atoms the 2 violated constraints over `rule_incomplete_provenance`(C6). Capture the cvc5 proof once to `examples/research_kernel/fixtures/cvc5_norm_conflict.proof` via `cvc5 --produce-proofs --dump-proofs logic/smt/norm_conflict.smt2` (recorded evidence; the live re-run check in 0.9.4 is structural, not byte-equality). Add `pub fn load_recorded_outcomes() -> RecordedOutcomes` (`include_str!`) to `src/verdict.rs`. `tests/recorded.rs`: copy `crates/ckc-compile/tests/manifest.rs` whole (the `golden_suite!` macro + `check_golden`/`check_roundtrip`/`check_schema` + path helpers + `write_type`/`#[ignore] regenerate`) and golden-lock `RecordedOutcomes` from `load_recorded_outcomes()` into `schemas/golden/verifier_outcomes.json` + `schemas/verifier_outcomes.schema.json`; add a load test asserting 11 entries with the per-entry `(solver,status,certificate_class)` above. Gate: `cargo test -p ckc-verify` recorded tests pass; golden matches; all workspace tests pass. (147K/200K)
  - [x] 0.9.3 Runner core (binary guard + harness) + z3 SMT family: add `crates/ckc-verify/src/runner.rs` (`pub mod runner;`) with `pub fn solver_available(bin: &str) -> bool` (spawn `bin --version`/`-version`, return false on spawn error — the repo's first binary-guard idiom, so solver-less CI stays green), `pub struct RunResult { stdout: String, stderr: String, exit_code: i32 }`, and `pub fn run_z3(artifact_abs: &Path) -> anyhow::Result<RunResult>`. Parse z3 stdout into a `VerdictStatus` (`unsat`→Unsat, `sat`→Sat) plus an objective extractor detecting the `( 1)` block in `repair_maxsmt`. Add `pub fn assert_matches_recorded(live: &VerifierOutcome, recorded: &VerifierOutcome)` (compare solver+status, and objective for repair). z3 is at `/usr/bin/z3` (base PATH). `tests/live_z3.rs` (PATH-guarded): for `logic/smt/{norm_conflict,decision_table,repair_maxsmt}.smt2` resolved under the workspace root, `eprintln!` and early-return when `!solver_available("z3")`, else run z3 and assert each live status equals the matching `load_recorded_outcomes()` entry (norm=unsat, dt=sat, repair=sat+obj 1). Gate: `cargo test -p ckc-verify --test live_z3` passes (runs z3 here; auto-skips if absent); all workspace tests pass. (98K/200K)
  - [x] 0.9.4 Runner: cvc5 proof production: in `runner.rs` add `pub fn run_cvc5_proof(artifact_abs: &Path) -> anyhow::Result<RunResult>` invoking `cvc5 --produce-proofs --dump-proofs <path>` (cvc5 at `~/.local/bin/cvc5`, base PATH). Detect `unsat` as the first stdout token and a proof block via a `:rule` substring. `tests/live_cvc5.rs` (guard `solver_available("cvc5")`): run cvc5 on `logic/smt/norm_conflict.smt2`; assert first token `unsat`, stdout contains `:rule` (proof present), exit 0, and that the recorded cvc5 outcome has `proof_present=true` and class `C6ProofObject`. Structural presence only — make no byte-equality assertion against the committed `cvc5_norm_conflict.proof` (cvc5 proof text is not guaranteed byte-stable; the committed proof is recorded evidence). Gate: `cargo test -p ckc-verify --test live_cvc5` passes (auto-skips if absent); all workspace tests pass. (99K/200K)
  - [x] 0.9.5 Runner: clingo ASP family: in `runner.rs` add `pub fn run_clingo(artifact_abs: &Path) -> anyhow::Result<RunResult>`. clingo exits 30 (10|20 bits) on SAT, so treat exit ∈ {10,30} as success and parse the `SATISFIABLE` token + the model-atom line. clingo at `/usr/bin/clingo`. `tests/live_clingo.rs` (guard `solver_available("clingo")`): run on `logic/asp/defeasible.lp` (assert stdout `SATISFIABLE` + atoms `defeated(rule_sepsis_bl_recommend)`, `in(arg_rule_bl_anaphylaxis_contra)`, `defeated_arg(arg_rule_sepsis_bl_recommend)`) and `logic/asp/event_calculus.lp` (assert `SATISFIABLE` + `holdsAt(allergy_known,10)` + `violation(conflict_ec_allergy_persistence)`); never assert exit 0. Compare both against their recorded outcomes. Gate: `cargo test -p ckc-verify --test live_clingo` passes (auto-skips if absent); all workspace tests pass. (93K/200K)
  - [x] 0.9.6 Runner: souffle + lean: in `runner.rs` add `pub fn run_souffle(artifact_abs: &Path, cwd: &Path) -> anyhow::Result<RunResult>` (run souffle from a caller-supplied scratch CWD — souffle writes `cycle.csv` relative to CWD) and `pub fn run_lean(artifact_abs: &Path) -> anyhow::Result<RunResult>` (lean at `~/.local/bin/lean`, base PATH; the single import-free file checks standalone, no lakefile/lake project). `tests/live_souffle_lean.rs` (guards `solver_available("souffle")`, `solver_available("lean")`): run souffle on `logic/datalog/priority.dl` from a `tempfile::TempDir` CWD, assert exit 0 and the produced `cycle.csv` is empty (`fs::metadata(...).len()==0`); run lean on `lean/Ckc/NormConflict.lean`, assert exit 0 and stdout+stderr contain no `error:` (kernel accepted, sorry/admit-free → C7). Compare against recorded (souffle `empty_relation` C4, lean `kernel_checked` C7). Gate: `cargo test -p ckc-verify --test live_souffle_lean` passes; repo untouched (tempdir CWD); all workspace tests pass. (109K/200K)
  - [x] 0.9.7 Runner: TLA+ SANY + Alloy: in `runner.rs` add `pub fn run_tla_sany(artifact_abs: &Path) -> anyhow::Result<RunResult>` (`java -cp $HOME/.local/share/tla/tla2tools.jar tla2sany.SANY <path>`) and `pub fn run_alloy(artifact_abs: &Path, cwd: &Path) -> anyhow::Result<RunResult>` (`java -jar $HOME/.local/share/alloy/alloy.jar exec <path>` from a scratch CWD — alloy writes a `<Module>/` dir relative to CWD). Guard via `solver_available("java")` plus `Path::exists` on each jar (resolve `$HOME` with `std::env::var("HOME")`). `tests/live_modelcheck.rs`: SANY on `logic/tla/Conflict.tla` → assert exit 0 + stdout contains `Semantic processing of module Conflict` + contains neither `error` nor `Abort`; alloy on `logic/alloy/Priority.als` from a `TempDir` CWD → assert stdout has a `check NoPriorityCycle` line ending in `UNSAT`. Compare against recorded (tla `semantic_check_passed` C4, alloy `no_counterexample` C4). Gate: `cargo test -p ckc-verify --test live_modelcheck` passes (auto-skips if java/jars absent); all workspace tests pass. (115K/200K)
  - [x] 0.9.8 Certificate builder: add `crates/ckc-verify/src/certificate.rs` (`pub mod certificate;`) with `pub fn certificate_for(target: &CompiledTarget, outcome: &VerifierOutcome) -> Certificate` and `pub fn certificates(bundle: &CompileBundle) -> Vec<Certificate>` returning 10 certs (the 9 `compile_all` targets zipped with their recorded outcomes by `ARTIFACT_PATHS` order, plus the standalone cvc5 cert over the norm_conflict target). Each Certificate: deterministic `certificate_id` `cert_<solver>_<artifact-stem>` (e.g. `cert_z3_norm_conflict`, `cert_cvc5_norm_conflict`, `cert_lean_norm_conflict` — solver prefix keeps the three norm_conflict certs distinct); `certificate_class = outcome.certificate_class`; `input_artifact_hashes = [content_hash(target)]` concatenated with `target.source_artifact_hashes`; `compiler_hash = None`; `solver_or_checker` = the `SolverId` snake_case string; `command_manifest = json!({"command": <runner cmd string>, "args": [...]})`; `result` = the `outcome.status` token (append the objective for repair); `proof_artifact_hashes = [content_hash(<committed cvc5 proof string>)]` for the cvc5 cert else `[]`; `replay_status = ReplayStatus::Passed`; `diagnostics = []`. Run `ckc_core::nf::normalize_all` so each Certificate's hash vecs are sorted (its Normalize impl exists). `tests/certificate.rs`: assert the cvc5 cert is `C6ProofObject` with exactly one proof hash, the lean cert is `C7Kernel`, the other 8 are `C4Executable`; determinism (two `certificates()` calls → equal per-index `content_hash`). Gate: `cargo test -p ckc-verify` certificate tests pass; deterministic; all workspace tests pass. (157K/200K)
  - [x] 0.9.9 ExecutionWitness builder: add `crates/ckc-verify/src/witness.rs` (`pub mod witness;`) with `pub fn witness_for(target: &CompiledTarget, outcome: &VerifierOutcome, bundle: &CompileBundle) -> ExecutionWitness` and `pub fn witnesses(bundle: &CompileBundle) -> Vec<ExecutionWitness>` (one per compile/cvc5 outcome → 10). Map each: `source_span_ids` = the union of `target.compilation_map` entry span ids (Normalize sorts/dedups); `applicable_rules`/`defeated_rules` for the defeasible ASP outcome (`applicable=[rule_sepsis_bl_recommend, rule_bl_anaphylaxis_contra]`, `defeated=[rule_sepsis_bl_recommend]` from the model atom); `violated_constraints` for the event-calculus outcome (`["conflict_ec_allergy_persistence"]`) and for the norm-conflict unsat (`["recommend_administer_beta_lactam AND prohibit_administer_beta_lactam"]`); `models` = `outcome.salient_atoms` as JSON for sat/satisfiable outcomes, `unsat_cores` = JSON for the norm-conflict unsat; `bundle_id` a fixed toy id (e.g. `bundle_research_kernel`); `certificate_ids` = the matching `certificate_for` id. Run `normalize_all` so each `witness_id` is the stable pass-12 id. `tests/witness.rs`: assert every witness has non-empty `source_span_ids` (acceptance: every verifier output maps to source spans); the defeasible witness has `defeated_rules == [rule_sepsis_bl_recommend]`; the EC witness `violated_constraints` contains `conflict_ec_allergy_persistence`; determinism. Gate: `cargo test -p ckc-verify` witness tests pass; every witness maps to ≥1 source span; all workspace tests pass. (149K/200K)
  - [x] 0.9.10 SHACL certificate + witness: in `certificate.rs`/`witness.rs` add `pub fn shacl_certificate(bundle: &CompileBundle) -> (Certificate, ExecutionWitness)` calling `ckc_term::shacl::validate_rules(&bundle.rules)`, which returns the `ShaclReport` with exactly 2 violations for `rule_incomplete_provenance` (missing `source_span_ids` MinCount(1) + empty `provenance` MinLength(1) — scenario 6). Build a `C6ProofObject` Certificate (`certificate_id="cert_shacl_rules"`, `solver_or_checker="ckc-shacl"`, `result="violations_found:2"`, `proof_artifact_hashes=[content_hash(&shacl_report)]`, `replay_status=Passed`) and an `ExecutionWitness` whose `violated_constraints` lists the 2 constraint ids and whose `source_span_ids` records the rule under review (`rule_incomplete_provenance` has empty spans, so reference the rule via `applicable_rules` and note the absent provenance in `violated_constraints`). `tests/shacl_cert.rs`: assert the report has 2 violations, the cert is `C6ProofObject` with one proof hash, and determinism over two calls. Gate: `cargo test -p ckc-verify` shacl tests pass; scenario-6 missing-provenance yields a C6 certificate; all workspace tests pass. (123K/200K)
  - [x] 0.9.11 Portfolio agreement + backend-disagreement diagnostic: add `crates/ckc-verify/src/portfolio.rs` (`pub mod portfolio;`) with `pub struct AgreementRecord { claim_id: String, backends: Vec<SolverId>, agreed_result: String, certificate_class: CertificateClass }` (derive Serialize/Deserialize/JsonSchema/PartialEq) and `pub fn portfolio_check(outcomes: &[VerifierOutcome]) -> (Vec<AgreementRecord>, Vec<CompileDiagnostic>)`. Cluster by the underlying CKC claim (keyed on the rule-id set / conflict id, NOT `artifact_path`): the norm-conflict claim `{Z3 unsat, Cvc5 unsat, Lean kernel_checked}` agree → one `C5Portfolio` `AgreementRecord` (claim_id `conflict_norm_bl_contradiction`); the priority-acyclicity claim `{Souffle empty_relation, Alloy no_counterexample}` agree → one `C5Portfolio` record (claim_id `priority_acyclicity`). Add `fn disagreement_diagnostic(claim_id: &str, conflicting: &[VerifierOutcome]) -> CompileDiagnostic` (code `backend_disagreement`, JA/EN messages, the affected spans) emitted when a claim's backends return contradictory statuses. `tests/portfolio.rs`: over the real recorded outcomes assert exactly two `C5Portfolio` agreement records and zero diagnostics; feed a synthetic divergent pair (the real `Z3 unsat` plus a fabricated `Z3 sat` for the same claim) and assert one `backend_disagreement` `CompileDiagnostic`. Gate: `cargo test -p ckc-verify` portfolio tests pass; both agreement and disagreement branches covered; all workspace tests pass. (140K/200K)
  - [ ] 0.9.12 Certificate graph assembly: add `crates/ckc-verify/src/graph.rs` (`pub mod graph;`) with `pub struct CertNode { artifact_hash: ContentHash, kind: String, certificate_class: Option<CertificateClass> }`, `pub struct CertEdge { from: ContentHash, to: ContentHash, relation: String }`, `pub struct CertificateGraph { nodes: Vec<CertNode>, edges: Vec<CertEdge> }` (derive Clone/Debug/PartialEq/Serialize/Deserialize/JsonSchema) and `pub fn build_graph(bundle: &CompileBundle, certs: &[Certificate], witnesses: &[ExecutionWitness]) -> CertificateGraph` linking each Certificate node → its CompiledTarget node (`relation="checks"`) → each bundle source-artifact node (`relation="derived_from"`), and Certificate → ExecutionWitness (`relation="witnessed_by"`). Sort `nodes` and `edges` deterministically (by hash/relation). `tests/graph.rs`: copy the `golden_suite!` harness and golden-lock `build_graph(...)` into `schemas/golden/certificate_graph.json` + `schemas/certificate_graph.schema.json` via `#[ignore] regenerate`; assert every Certificate hash appears as a node and the graph has no self-reachable node (acyclic). Gate: `cargo test -p ckc-verify` graph tests pass; golden matches; all workspace tests pass.
  - [ ] 0.9.13 Assurance seed: add `crates/ckc-verify/src/assurance.rs` (`pub mod assurance;`) with transparent newtype `AssuranceSeed(pub Vec<AssuranceNode>)` and `pub fn assurance_seed(certs: &[Certificate]) -> AssuranceSeed` building a GSN-style tree: root `node_type="goal"` whose `claim` is the SPEC §17 top research assurance claim ("accepted CKC artifacts are source-grounded, deterministic, formally checkable, replayable, and suitable for identifying source text requiring review"), with child `node_type="strategy"` nodes for grounding / determinism / formal-checkability, each carrying `evidence_artifact_ids` = the relevant certificate `content_hash`es and `status="supported"`. Use deterministic `AssuranceNodeId`s (e.g. `asn_root`, `asn_grounding`); run `normalize_all`. `tests/assurance.rs`: golden-lock `assurance_seed(...)` into `schemas/golden/assurance_seed.json` + schema via `#[ignore] regenerate`; assert the root has ≥1 child and every strategy node's `evidence_artifact_ids` is non-empty. Gate: `cargo test -p ckc-verify` assurance tests pass; golden matches; all workspace tests pass.
  - [ ] 0.9.14 Orchestration `verify_all`: in `src/lib.rs` add `pub struct VerificationReport { certificates: Vec<Certificate>, witnesses: Vec<ExecutionWitness>, agreements: Vec<AgreementRecord>, disagreements: Vec<CompileDiagnostic>, graph: CertificateGraph, assurance: AssuranceSeed }` and `pub fn verify_all(bundle: &CompileBundle) -> VerificationReport` composing 0.9.8–0.9.13 from the recorded outcomes: `certificates` = `certificates(bundle)` (10) plus the `shacl_certificate` cert (→ 11); `witnesses` = `witnesses(bundle)` (10) plus the shacl witness (→ 11); `agreements`/`disagreements` from `portfolio_check(&load_recorded_outcomes().0)`; `graph` from `build_graph`; `assurance` from `assurance_seed`. `tests/verify_all.rs`: assert counts (11 certificates, 11 witnesses, 2 agreements, 0 disagreements) and that two `verify_all(&CompileBundle::load_toy())` runs produce equal per-field `content_hash` vectors (determinism). Gate: `cargo test -p ckc-verify` verify_all tests pass; deterministic; all workspace tests pass.
  - [ ] 0.9.15 Committed verification artifact files + byte-identical regeneration: create top-level `certs/`. Add an `#[ignore = "regenerate-only"]` writer test in `tests/verify_all.rs` (mirror `crates/ckc-compile/tests/portfolio.rs`) that writes each `verify_all` artifact to a canonical path under `certs/` via `to_canonical_bytes` — `certs/certificates/<certificate_id>.json` (×11), `certs/witnesses/<witness_id>.json` (×11), `certs/certificate_graph.json`, `certs/assurance_seed.json` — and copies the recorded `cvc5_norm_conflict.proof`; commit those files. Add a non-ignored gate test asserting each committed file's bytes equal the corresponding live `verify_all` artifact (regeneration is byte-identical). Gate: `cargo test -p ckc-verify` file-equality test passes; committed `certs/*` regenerate byte-identically; all workspace tests pass.
  - [ ] 0.9.16 Verification-manifest golden: in `src/lib.rs` define `pub struct VerificationEntry { artifact_kind: String, artifact_path: String, content_hash: ContentHash }` and transparent newtype `VerificationManifest(pub Vec<VerificationEntry>)` (derive Clone/Debug/PartialEq/Serialize/Deserialize/JsonSchema), plus `pub fn verification_manifest(bundle: &CompileBundle) -> VerificationManifest` pairing each `verify_all` artifact (certificates, witnesses, graph, assurance — in the deterministic order of 0.9.15) with its `certs/` path and the artifact's `content_hash`. `tests/manifest.rs`: copy the `golden_suite!` harness and golden-lock `verification_manifest(&CompileBundle::load_toy())` into `schemas/golden/verification_manifest.json` + `schemas/verification_manifest.schema.json` via `#[ignore] regenerate`; this one compact golden byte-locks every verification artifact through its hash. Gate: `cargo test -p ckc-verify` manifest tests pass; golden matches; all workspace tests pass.
  - [ ] 0.9.17 CAS persistence + cross-run determinism: in `crates/ckc-verify/tests/persistence.rs` run `verify_all(&CompileBundle::load_toy())`, wrap each Certificate (`ArtifactKind::Certificate`), ExecutionWitness (`ArtifactKind::ExecutionWitness`), and AssuranceNode (`ArtifactKind::AssuranceNode`) via `ArtifactEnvelope::wrap` (meta stage `"verify"`, placeholder all-zeros `content_hash`, `semantic_profiles` reflecting each artifact's profiles, `replay_command: Some("ckc demo research-kernel --replay --out runs/research")`) and `ContentStore::put_batch` into a `tempfile::TempDir`; assert each `exists` and `verify`s and each envelope `content_hash` == `content_hash(artifact)`. Run the load→verify_all→wrap pipeline twice over two independent temp stores and assert per-artifact `envelope_hash`/inner `content_hash`/store keys match across runs — compare per-artifact hashes, not raw `StoreManifest` bytes (`stored_at_epoch` differs by design; mirror `crates/ckc-compile/tests/persistence.rs` and ckc-store `cas_manifest_hash_is_stable`). Gate: `cargo test -p ckc-verify` persistence tests pass; every verification artifact stored/verified/content-hashed; per-artifact hashes match across two runs; `scripts/ci.sh` green; all workspace tests pass.
- [ ] 0.10 Conflict detection (SPEC 15)
- [ ] 0.11 CLI commands (SPEC 18)
- [ ] 0.12 Report and UI
- [ ] 0.13 Replay and determinism
