# CKC Roadmap

Phases contain top-level tasks; top-level tasks may contain indented subtasks.
See `.agent/prompt.md` for the session workflow that governs progression.

## Phase 0: Proof-Carrying Research Kernel

- [x] 0.1 Project scaffold
- [x] 0.2 CKC schema v0 (SPEC 10)
  - [x] 0.2.1 Foundation: workspace deps (serde, sha2, schemars), ID newtypes for every SPEC 10 object, SemanticProfile enum (SPEC 9), shared enums (BindingStatus, CertificateClass, ConflictClassification, Severity, etc.), RFC 8785 canonical JSON serializer, content-hash fn (sha256 of canonical bytes). Gate: `cargo test -p ckc-core` passes; canonical JSON determinism and ID serde round-trip tests pass.
  - [x] 0.2.2 Source and terminology types: CorpusDocument, SourceSpan, ExtractedTable, Concept, TerminologyBinding (exact fields per SPEC 10). Gate: serde round-trip tests; canonical JSON byte stability for one fixture instance of each type.
  - [x] 0.2.3 Evidence and clinical formalization types: PICOFrame, EtDFrame, EvidenceAtom, Norm, Action, Rule, ClinicalClaim (exact fields per SPEC 10). Gate: serde round-trip; profiles field validates against SemanticProfile.
  - [x] 0.2.4 Structured artifact types: DecisionTable, DecisionRow, WorkflowFragment, EventNarrative, PatientCase, ExecutionWitness (exact fields per SPEC 10). Gate: serde round-trip tests.
  - [x] 0.2.5 Verification and assurance types: Conflict, ArgumentGraph, Certificate, AssuranceNode, AuditTrace (exact fields per SPEC 10). Gate: serde round-trip tests.
  - [x] 0.2.6 JSON Schema generation and golden byte tests: generate JSON Schema from all Rust types (schemars), write golden canonical JSON fixtures covering every type, verify content hashes are deterministic across serialization round-trips. Gate: `cargo test -p ckc-core` passes; golden files committed; schema files committed.
- [x] 0.3 Normal Form (SPEC 11)
  - [x] 0.3.1 NF pipeline scaffold and text normalization (passes 1-2): create `nf.rs` module in ckc-core. Define `NfContext` (rewrite log, diagnostic accumulator) and `Normalize` trait dispatching on SemanticProfile. Pass 1: preserve raw_text/raw fields verbatim. Pass 2: Unicode NFKC, Japanese punctuation normalization (full-width ↔ half-width), whitespace collapse on derived text fields (nfkc_text, search_text, display_text, gloss_ja, gloss_en, labels). Add `unicode-normalization` workspace dep. Gate: `cargo test -p ckc-core` passes; unit tests confirm Japanese text normalization and raw field preservation.
  - [x] 0.3.2 Structural normalization (passes 3-5): Pass 3: alpha-normalize variable names in Rule antecedent/consequent/context `Value` trees using stable canonical renaming. Pass 4: sort `source_span_ids`, `evidence_atoms`, commutative `and`/`or` operands, and unordered set fields by canonical comparison. Pass 5: preserve order for temporal sequences, priority chains, decision-table rows (order-sensitive hit policies), and workflow transitions. Gate: two Rules differing only in commutative antecedent order produce identical NF canonical bytes and digest.
  - [x] 0.3.3 Domain normalization stubs (passes 6-8): Pass 6: normalize Action constructors (canonical action_type casing, sort parameter keys). Pass 7: normalize quantities (numeric precision, unit string to canonical UCUM form). Pass 8: terminology normalization trait stub (identity transform until 0.6 e-graph integration). Gate: Action/quantity fixtures normalize deterministically; pass 8 compiles as identity.
  - [x] 0.3.4 Complex structure canonicalization (passes 9-11): Pass 9: Japanese clinical modality lexicon stub (minimal phrase → deontic-projection map for toy scenarios, preserving original_modality_phrase_ja). Pass 10: canonicalize DecisionTable — sort rows for non-order-sensitive hit policies, normalize cell `Value` trees. Pass 11: canonicalize ArgumentGraph (stable node/edge ordering by IDs) and WorkflowFragment (stable state/transition ordering by IDs). Gate: DecisionTable with shuffled rows (UNIQUE policy) yields identical NF; ArgumentGraph with shuffled edges yields identical NF.
  - [x] 0.3.5 Stable IDs, diagnostic ordering, and idempotency proofs (passes 12-13 + invariants): Pass 12: generate deterministic stable IDs from normalized content hash + source anchor IDs. Pass 13: sort accumulated diagnostics by (stage, code, span). Property-based tests (`proptest`): NF(NF(x)) == NF(x) for Rule, ClinicalClaim, DecisionTable, ArgumentGraph. Golden NF fixtures for all types with committed expected bytes. Gate: `cargo test -p ckc-core` passes; proptest idempotency passes; golden NF files committed.
- [x] 0.4 Content-addressed store
  - [x] 0.4.1 ArtifactEnvelope and metadata types (ckc-core): define `ArtifactMeta` with SPEC 5.2 metadata fields (schema_version, producer_version, command_manifest, source_input_hashes, parent_hashes, stage, semantic_profiles, content_hash, certificate_ids, replay_command), `ArtifactKind` enum discriminating all storable CKC object types, `ArtifactEnvelope` combining kind + meta. Canonical JSON serialization and content hashing of envelopes (envelope hash = store key; inner artifact hash stored in meta). Serde round-trip and canonical determinism tests. Gate: `cargo test -p ckc-core` passes; envelope wrapping a Rule fixture produces stable canonical bytes and content hash.
  - [x] 0.4.2 Filesystem CAS (ckc-store): implement `ContentStore` with configurable root path. Operations: `put` (serialize envelope to canonical JSON, compute content hash, write to `<root>/objects/<hex[0:2]>/<hex[2:4]>/<hex>.json`), `get` (read by ContentHash, deserialize), `exists`, `verify` (re-hash stored bytes and confirm match). Add workspace deps as needed (e.g., `tempfile` for test fixtures). Gate: `cargo test -p ckc-store` passes; round-trip put/get/verify tests pass on multiple artifact types; verified integrity check detects corruption.
  - [x] 0.4.3 Store manifest and batch operations: define `StoreManifest` listing all stored artifact hashes with kind, stage, profiles, and timestamp. `generate_manifest()` walks the store deterministically (sorted by hash). `put_batch()` stores multiple envelopes. Manifest is itself content-hashed and storable. Integration test: create diverse artifacts, generate manifest, verify manifest hash is identical across re-generation. Gate: `cargo test -p ckc-store` passes; manifest determinism test passes; manifest committed as golden fixture.
- [x] 0.5 Toy fixtures (SPEC 20 Phase 0 scenarios)
  - [x] 0.5.1 Toy source corpus (documents, spans, tables): create the source-grounding substrate for all 8 SPEC 20 toy scenarios. Three CorpusDocument fixtures: (a) sepsis management guideline, (b) drug allergy/anaphylaxis reference, (c) vital-sign assessment protocol. ~15 SourceSpan fixtures covering: recommendation text (推奨する), contraindication text (禁忌である), evidence summary, terminology variant mentions (βラクタム, ベータラクタム, β-ラクタム), vital-sign table cells (体温, 心拍数, 血圧 with numeric thresholds), allergy history text, provenance metadata text. One ExtractedTable for the vital-sign table with cell-level spans. All spans have realistic JA raw_text with NFKC-normalized variants in derived fields. Write canonical JSON fixtures to `examples/toy_research_kernel/fixtures/`. Integration test validates: all spans reference existing documents, span chains (previous/next) are consistent, table cell spans exist, canonical hashes are deterministic across regeneration. Gate: tests pass; JSON fixtures committed; all 8 scenarios have source-span coverage.
  - [x] 0.5.2 Clinical formalization fixtures (rules, norms, claims, PICO/EtD): create the clinical content layer for scenarios 1 (norm conflict), 5 (repair), 6 (SHACL), 7 (Lean proof). Two opposing Rules with Norms and Actions: (a) sepsis beta-lactam recommendation (direction: For, kind: Defeasible, strength: Strong, certainty: Moderate, JA modality: 投与を推奨する), (b) beta-lactam anaphylaxis contraindication (direction: Against, kind: Defeasible, strength: Strong, JA modality: 禁忌である). PICOFrame for the sepsis CQ. EtDFrame with GRADE dimensions. EvidenceAtom linking to source evidence spans. Two ClinicalClaims wrapping the rules. A deliberately provenance-incomplete Rule for SHACL scenario 6 (empty source_span_ids, empty provenance string). All objects reference source spans from 0.5.1. Integration test validates: referential consistency with 0.5.1 spans, canonical hash determinism, NF normalization produces expected output, provenance-incomplete rule is identifiable by empty source_span_ids. Gate: tests pass; JSON fixtures committed.
  - [x] 0.5.3 Terminology concepts and variant fixtures: create the terminology layer for scenario 2 (e-graph variant convergence). Concept fixtures: beta-lactam (βラクタム系抗菌薬) with TerminologyBindings (MEDIS, HOT, ICD-10), sepsis (敗血症), anaphylaxis (アナフィラキシー), vital-sign concepts (体温, 心拍数, 血圧). Beta-lactam variant Concepts sharing semantic_type and egraph_class_id: βラクタム, ベータラクタム, β-ラクタム, beta-lactam, plus one brand-name variant. Each concept has 1+ TerminologyBinding with appropriate BindingStatus (Exact, Broad, Related). All concepts reference source spans from 0.5.1. Integration test validates: all variant concepts share the same egraph_class_id, all concepts reference existing spans, all bindings have valid status, canonical hash determinism. Gate: tests pass; JSON fixtures committed; variant set is sufficient for e-graph convergence testing in 0.6.
  - [x] 0.5.4 Decision table, Event Calculus, and patient case fixtures: create the structured-artifact layer for scenarios 3 (decision table), 4 (Event Calculus), and witness contexts. DecisionTable with Unique hit policy, 4+ vital-sign rows where two rows have overlapping conditions (same temperature/heart-rate range) with incompatible outputs, plus a gap (a value range with no matching row). EventNarrative modeling allergy persistence: detect_allergy at t=0 initiates allergy_known fluent, administer_drug happens at t=10, holds_query confirms allergy_known=true at t=10 (violation setup). Two synthetic PatientCase fixtures: (a) sepsis + beta-lactam allergy (shared witness for scenario 1), (b) sepsis without allergy (non-conflicting baseline). WorkflowFragment for the sepsis treatment pathway. All reference source spans from 0.5.1. Integration test validates: decision table has provable overlapping rows (two rows with non-disjoint conditions and different outputs), event narrative temporal ordering is consistent (happens times are non-negative, initiates reference valid events), patient cases cover both conflict and non-conflict paths. Gate: tests pass; JSON fixtures committed.
  - [x] 0.5.5 Conflict seeds, argumentation, assurance, and fixture bundle: create verification-layer seed fixtures and full-bundle integration for scenarios 5 (repair), 7 (Lean), and overall fixture coherence. Three Conflict fixtures: (a) norm contradiction (recommendation vs contraindication, severity: High, classification: TrueConflict), (b) decision-table overlap (severity: Medium), (c) temporal violation from EC allergy persistence (severity: High). ArgumentGraph for the sepsis/contraindication argumentation with attack, support, and defeat edges. Seed AssuranceNode fixtures (top goal + 2 strategy children + 2 solution leaves). ExecutionWitness referencing the conflict patient case. Bundle integration test: load ALL 0.5.x fixtures from JSON, validate complete cross-referential consistency (every referenced span_id, concept_id, rule_id, witness_id, argument_graph_id resolves to an object in the bundle), store all through CAS using put_batch, generate and verify a deterministic StoreManifest, NF-normalize all applicable types (Rule, ClinicalClaim, DecisionTable, ArgumentGraph) and verify idempotency. Gate: full bundle integration test passes; all fixtures committed under `examples/toy_research_kernel/fixtures/`; manifest hash is deterministic; fixture set covers all 8 SPEC 20 scenarios.
- [ ] 0.6 Terminology and e-graph
  - [x] 0.6.1 TerminologyGraph and concept indexing (ckc-term): populate the empty ckc-term crate. Define `TerminologyGraph` holding `Concept` and `TerminologyBinding` entries (reusing types from ckc-core) with in-memory indexes by concept_id, egraph_class_id, system+code, and label substring (ja/en). Operations: `insert`, `get_by_id`, `get_by_egraph_class`, `find_by_system_code`, `search_by_label`, `load_from_json` (deserialize a Vec<Concept> fixture). Add ckc-core as a dependency. Gate: `cargo test -p ckc-term` passes; all 11 toy concepts from 0.5.3 load and query correctly; variant concepts (5 beta-lactam entries) return together under `get_by_egraph_class("eclass_beta_lactam")`; search_by_label finds concepts by partial JA/EN match.
  - [x] 0.6.2 E-graph equivalence engine: add `egg` workspace dependency (prefer egg for programmatic Rust API; egglog as alternative if declarative rules prove clearer). Define a CKC term language for egg with node types: ConceptRef(ConceptId), Label(String), EgraphClass(String). Rewrite rules: (a) concepts sharing egraph_class_id are equivalent, (b) Japanese variant patterns (βラクタム ↔ ベータラクタム ↔ β-ラクタム via label normalization), (c) brand-to-class subsumption (セフトリアキソン narrower-than βラクタム class). Cost function: prefer the canonical concept (shortest concept_id or designated primary). `TermEquivalence` engine: load from TerminologyGraph, run bounded saturation, extract canonical representative per e-class, emit `EgraphArtifact` (JSON: class_ids, canonical representatives, rewrite count, saturation stats, iterations). Store EgraphArtifact through CAS. Gate: `cargo test -p ckc-term` passes; all 5 beta-lactam variant concepts converge to one e-class; EgraphArtifact is deterministic and content-hashed; saturation terminates within bounded iterations.
  - [ ] 0.6.3 NF pass 8 real implementation: replace the identity stub in `nf.rs` pass 8 with e-graph terminology normalization. `NfContext` gains an optional `&TermEquivalence` reference. When normalizing any CKC object containing concept_id references, terminology labels, or action target_concept fields, resolve through the e-graph canonical representative. Concept-bearing types affected: Rule (context/antecedent/consequent Value trees containing concept refs), ClinicalClaim (indirectly via rules), Action (target_concept), Norm (action), DecisionTable (cell values referencing concepts). Update existing NF golden fixtures to reflect terminology-normalized output. Existing pass 8 identity tests become convergence tests. Property test: two Rules identical except one references concept_bl_variant_katakana and the other concept_bl_variant_hyphenated produce identical NF canonical bytes. Gate: all existing `cargo test -p ckc-core` NF tests pass with updated goldens; new variant-convergence property test passes; pass 8 records rewrites in the rewrite log.
  - [ ] 0.6.4 RDF/SKOS/Turtle export and SHACL validation: add `oxrdf` + `oxttl` workspace dependencies for RDF/Turtle serialization. Implement `export_skos_turtle(&TerminologyGraph) -> String` in ckc-term: each Concept becomes a skos:Concept with skos:prefLabel (label_ja), skos:altLabel (label_en), terminology bindings as skos:exactMatch/broadMatch/narrowMatch/relatedMatch by BindingStatus, egraph_class_id as a shared skos:ConceptScheme or owl:equivalentClass grouping, and source provenance triples. Implement minimal SHACL-subset validator in ckc-term (NodeShape + sh:minCount constraint, sufficient for Phase 0): define shapes requiring Rule to have ≥1 source_span_id and non-empty provenance. Validate toy fixtures including the provenance-incomplete rule from 0.5.2 (scenario 6). Emit structured `ShaclReport` (violations with focus node, path, message, severity). Gate: RDF export round-trips through oxttl parse; SHACL catches provenance-incomplete rule; passing rules validate clean; ShaclReport is serializable and content-hashable; committed as fixture.
  - [ ] 0.6.5 Alignment coherence, integration, and committed artifacts: implement `check_alignment_coherence(&TerminologyGraph, &TermEquivalence)` in ckc-term: detect concepts sharing an egraph_class_id but having terminology bindings in the same system with different codes and no declared mapping relation between them; emit `AlignmentIncoherence` diagnostic with concept pair, conflicting bindings, and repair suggestion. Plant one incoherent binding in a test-only fixture to verify detection. Full integration test of 0.6 pipeline: load 0.5.x fixtures → build TerminologyGraph → run e-graph saturation → NF pass 8 on all Rules/Claims → RDF/SKOS export → SHACL validate → coherence check → store all artifacts (EgraphArtifact, RDF export, ShaclReport, AlignmentIncoherence diagnostics) via CAS → verify deterministic manifest. Gate: planted incoherent alignment detected; clean alignments pass; integration test passes end-to-end; all artifacts committed; manifest hash is deterministic across reruns.
- [ ] 0.7 Retrieval fixture
- [ ] 0.8 Compiler targets (SPEC 14)
- [ ] 0.9 Verification and certificates (SPEC 13)
- [ ] 0.10 Conflict detection (SPEC 15)
- [ ] 0.11 CLI commands (SPEC 18)
- [ ] 0.12 Report and UI
- [ ] 0.13 Replay and determinism
