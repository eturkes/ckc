# CKC Roadmap

Phases contain top-level tasks; top-level tasks may contain indented subtasks.
See `.agent/prompt.md` for the session workflow that governs progression.

Top-level task annotations:
- `@<short-hash>`: the Type A commit that opened the task; the start of its
  review range. The first roadmap item uses the initial commit.
- ` reviewed`: a Type R (per-task) or Type P (Phase) review has covered the task.

A trailing `(NNNK/200K)` on a completed subtask records the session's
context-window usage at completion, produced by `python3 .agent/context_window.py`
just before commit. Type A reads the prior top-level task's annotations to
calibrate new subtask granularity.

## Phase 0: Proof-Carrying Research Kernel

- [x] 0.1 Project scaffold @a9a3321 reviewed
- [x] 0.2 CKC schema v0 (SPEC 10) @8f0ae42 reviewed
- [x] 0.3 Normal Form (SPEC 11) @642fabb reviewed
- [x] 0.4 Content-addressed store @c96a48c reviewed
- [x] 0.5 Toy fixtures (SPEC 20 Phase 0 scenarios) @8933fdc reviewed
- [x] 0.6 Terminology and e-graph @f0151f5 reviewed
- [x] 0.7 Retrieval fixture @3a6d8c4 reviewed
- [x] 0.8 Compiler targets (SPEC 14) @24b00a2 reviewed
- [x] 0.9 Verification and certificates (SPEC 13) @aca8d79 reviewed
- [x] 0.10 Conflict detection (SPEC 15) @26da1a2 reviewed
- [x] 0.11 CLI commands (SPEC 18) @8413389 reviewed
- [x] 0.12 Report and UI @60daba3
  - [x] 0.12.1 ckc-report crate scaffold + bilingual report object model + schema export: add `crates/ckc-report` to root `Cargo.toml` `members`; its `Cargo.toml` `[dependencies]` `ckc-core`/`ckc-compile`/`ckc-verify`/`ckc-conflict` `{ path = "../…" }` + `serde`/`serde_json`/`schemars` `{ workspace = true }`. `src/lib.rs` defines the SPEC-21/23 bilingual report model (derive `Clone/Debug/PartialEq/Serialize/Deserialize/JsonSchema`; carry NO `Normalize` impl — determinism follows from deterministic assembly over already-normalized inputs, mirroring `VerificationReport`/`ConflictReport`), re-exporting `ckc_core::enums::{CertificateClass,Severity,ConflictClassification}`: `pub struct Report { command:String, producer_version:String, summary:ReportSummary, conflict_cards:Vec<ConflictCard> }`; `ReportSummary { n_documents/n_spans/n_claims/n_rules/n_conflicts:usize, certificate_depth_distribution:Vec<DepthCount>, conflict_taxonomy_counts:Vec<TaxonomyCount> }`; `DepthCount { certificate_class:CertificateClass, count:usize }`; `TaxonomyCount { conflict_type:String, count:usize }`; `ConflictCard { conflict_id:String, conflict_type:String, severity:Severity, classification:ConflictClassification, source_spans:Vec<CardSpan>, gloss_ja:String, gloss_en:String, normalized_view:serde_json::Value, explanation_ja:String, explanation_en:String, witness:Vec<serde_json::Value>, certificate_evidence:Vec<CardCertificate>, certificate_depth:Option<CertificateClass>, repair_candidates:Vec<serde_json::Value>, human_review_question_ja:String, human_review_question_en:String, adjudication_status:String }`; `CardSpan { span_id:String, raw_text:String, display_text:String, table_cell:Option<serde_json::Value>, language:String }`; `CardCertificate { certificate_id:String, certificate_class:CertificateClass, solver_or_checker:String }`. `tests/schema.rs` mirrors `crates/ckc-verify/tests/manifest.rs` `check_schema` + `#[ignore] regenerate`: assert `schemars::schema_for!(Report)` pretty bytes equal `schemas/report.schema.json` (run regen once, commit the schema). Gate: `cargo test -p ckc-report`; all workspace tests pass. (97K/200K)
  - [x] 0.12.2 report summary assembly + claim/document loaders: in `crates/ckc-report` add `CLAIMS_JSON`/`DOCUMENTS_JSON = include_str!("../../../examples/research_kernel/fixtures/{claims,documents}.json")` + `pub fn load_claims() -> Vec<ckc_core::clinical::ClinicalClaim>` / `load_documents() -> Vec<ckc_core::source::CorpusDocument>` (mirror `CompileBundle::load_toy`'s `serde_json::from_str(...).expect(...)`). Implement `pub fn build_summary(bundle:&CompileBundle, claims:&[ClinicalClaim], documents:&[CorpusDocument], verification:&VerificationReport, conflicts:&ConflictReport) -> ReportSummary`: `n_documents=documents.len()`, `n_spans=bundle.spans.len()`, `n_claims=claims.len()`, `n_rules=bundle.rules.len()`, `n_conflicts=conflicts.conflicts.len()`; `certificate_depth_distribution` = tally `verification.certificates` by `certificate_class` into `Vec<DepthCount>` sorted ascending by class (`CertificateClass: Ord`); `conflict_taxonomy_counts` = tally `conflicts.conflicts` by `conflict_type` into `Vec<TaxonomyCount>` sorted by `conflict_type`. `tests/summary.rs` over `CompileBundle::load_toy()`+`verify_all`+`detect_all` asserts `n_documents=3, n_spans=16, n_claims=2, n_rules=3, n_conflicts=3`; depth-distribution counts sum to 11 and include `{certificate_class:C7Kernel,count:1}`; taxonomy equals `[{decision_table_overlap,1},{norm_contradiction,1},{temporal_violation,1}]`. Gate: `cargo test -p ckc-report --test summary`; all workspace tests pass. (97K/200K)
  - [x] 0.12.3 conflict-card assembly (SPEC 21 card order): add `pub fn build_conflict_card(conflict:&Conflict, spans:&[SourceSpan], claims:&[ClinicalClaim], verification:&VerificationReport) -> ConflictCard` realizing the 10-element §21 order — (1) `source_spans`: map each `conflict.source_spans` `SpanId` to its `SourceSpan` in `spans` → `CardSpan{span_id,raw_text,display_text,table_cell:span.table_cell.as_ref().map(|c| serde_json::to_value(c).unwrap()),language}`, in conflict order; (2) `gloss_ja`/`gloss_en`: best-effort join over `claims` whose `source_span_ids` intersect `conflict.source_spans`, glosses joined by `" / "` (empty when none, e.g. decision-table cells); (3) `normalized_view = conflict.normalized_view.clone()`; (4) `explanation_ja`/`explanation_en`: deterministic bilingual template from `conflict_type`+`severity`+`classification` (distinct from element 9); (5) `witness = conflict.solver_evidence.clone()` (the inline sat-model / EC-model / overlap-witness — keep it inline, no join to `ExecutionWitness` by id); (6) `certificate_evidence`: each `verification.certificates` entry whose `content_hash` (`ckc_core::canonical::content_hash`) is in `conflict.minimal_artifact_set` — the real per-conflict evidence linkage already computed by `ckc_conflict::link::minimal_artifact_set` in task 0.10 — mapped to `CardCertificate{certificate_id,certificate_class,solver_or_checker}`, sorted by `certificate_id` (resolving the conflict's own declared hash set keeps one source of truth, with no new mapping and no lossy `solver_or_checker` filter, since `z3` backs 3 certs and `clingo` 2); (7) `certificate_depth = certificate_evidence.iter().map(|c| c.certificate_class).max()`; (8) `repair_candidates = conflict.repair_candidates.clone()`; (9) `human_review_question_ja/en` = the conflict's; (10) `adjudication_status = "pending_adjudication"`. `tests/card.rs` over `detect_all(...).conflicts` (key on `conflict_type` — ids are content-addressed `nf-…`): `norm_contradiction` card → `source_spans.len()==2` including the `span_rec_sepsis_bl` `CardSpan` whose `raw_text` contains `敗血症`, `gloss_en` non-empty (claim_sepsis_bl_recommend + claim_bl_contra), `certificate_evidence` holds the 4 certs `{cert_clingo_defeasible,cert_cvc5_norm_conflict,cert_lean_norm_conflict,cert_z3_norm_conflict}` (solvers `clingo`/`cvc5`/`lean`/`z3`) with `certificate_depth==Some(C7Kernel)`, `witness.len()==1`, `repair_candidates.len()==2`; `decision_table_overlap` card → `gloss_en` empty, `certificate_evidence==[cert_z3_decision_table]` (solver `z3`) with `certificate_depth==Some(C4Executable)`, `repair_candidates.len()==2`; `temporal_violation` card → `certificate_evidence==[cert_clingo_event_calculus]` (solver `clingo`) with `certificate_depth==Some(C4Executable)`, `repair_candidates.len()==1`. Gate: `cargo test -p ckc-report --test card`; all workspace tests pass. (120K/200K)
  - [x] 0.12.4 top-level report assembly + committed report.json golden: implement `pub fn assemble_report(bundle:&CompileBundle, claims:&[ClinicalClaim], documents:&[CorpusDocument], verification:&VerificationReport, conflicts:&ConflictReport) -> Report` — `command="ckc demo research-kernel --out runs/research"` (match `pipeline::DEMO_COMMAND`), `producer_version=env!("CARGO_PKG_VERSION")`, `summary=build_summary(...)`, `conflict_cards = conflicts.conflicts.iter().map(|c| build_conflict_card(c,&bundle.spans,claims,verification)).collect()` then sorted by `(conflict_type,conflict_id)` — plus `pub fn assemble_toy_report() -> Report` = assemble over `load_toy()`+`load_claims()`+`load_documents()`+`verify_all`+`detect_all`. Run a regen to write `examples/research_kernel/fixtures/report.json = to_canonical_bytes(&assemble_toy_report())` and commit it. `tests/golden.rs` mirrors `ckc-compile/tests/manifest.rs` `check_golden` + `#[ignore] regenerate`: assert `to_canonical_bytes(&assemble_toy_report())` byte-equals committed `fixtures/report.json`; structural sanity (`conflict_cards.len()==3`, `summary.n_conflicts==3`). Compare `Report` only by byte-golden and `content_hash` (serde_json's f64 parse/format asymmetry — agent-memory 2026-05-29 — can break a deserialize→PartialEq roundtrip at ULP boundaries, so the byte-golden + regen idiom is the determinism guard). Gate: `cargo test -p ckc-report --test golden`; all workspace tests pass. (90K/200K)
  - [x] 0.12.5 run_report stage + ckc report + demo wiring: add `ckc-report = { path = "../ckc-report" }` to `crates/ckc-cli/Cargo.toml` and re-export `ckc_report::{Report,assemble_report,load_claims,load_documents}` in `src/lib.rs`. In `src/pipeline.rs` implement `pub fn run_report(bundle:&CompileBundle, claims:&[ClinicalClaim], documents:&[CorpusDocument], verification:&VerificationReport, conflicts:&ConflictReport, out_dir:&Path) -> anyhow::Result<Vec<RunManifestEntry>>`: `assemble_report(...)`, `write_artifact(out_dir,"report.json",&to_canonical_bytes(&report))`, return one `RunManifestEntry{stage:"report",artifact_kind:"report",artifact_path:"report.json",content_hash:content_hash(&report)}`. Refactor `run_conflicts` to accept the already-detected `&ConflictReport` (compute `detect_all` once); `run_pipeline` becomes compile → verify (keep `report`) → `let conflicts = detect_all(&bundle,&report)` → `run_conflicts(&bundle,&conflicts,out_dir)` → `run_substrate` → `run_report(&bundle,&load_claims(),&load_documents(),&report,&conflicts,out_dir)`, appending the report entry last (totals 9+24+4+3+1=41). Add `Command::Report{bundle,out}` to `main.rs` (`verify_all`+`detect_all` then `run_report`) and update the `Conflicts` handler + `tests/conflicts.rs` for the `run_conflicts` signature change. `tests/report.rs`: run the demo path into a `TempDir`, assert `out_dir/report.json` exists and byte-equals committed `examples/research_kernel/fixtures/report.json`, with determinism over two runs (equal `content_hash`). Update `tests/demo.rs` entry-count to 41. Gate: `cargo test -p ckc-cli`; `scripts/ci.sh` green; all workspace tests pass. (149K/200K)
  - [x] 0.12.6 SvelteKit static-app scaffold (ui/sveltekit-app, SPEC 19): create the read-only bilingual UI app — `package.json` (`@sveltejs/kit`, `svelte`, `@sveltejs/adapter-static`, `vite`, `typescript`, `svelte-check`; scripts `dev`/`build`/`preview` + a `prebuild` that copies `examples/research_kernel/fixtures/report.json` to `static/report.json`), `svelte.config.js` (adapter-static, `prerender` default, fallback off), `vite.config.ts`, `tsconfig.json`, `src/app.html`, `src/routes/+layout.ts` (`export const prerender = true`), `src/routes/+page.svelte` (placeholder "CKC Report" heading). Commit `pnpm-lock.yaml`; extend `.gitignore` with `ui/sveltekit-app/static/report.json` (build-copied from the single committed source — `node_modules/`, `.svelte-kit/`, `build/` are already ignored). The UI build stays out of `scripts/ci.sh` and the determinism manifest (Phase-0 decision). Gate: `cd ui/sveltekit-app && pnpm install --frozen-lockfile && pnpm build` exits 0 and writes `build/index.html`. (105K/200K)
  - [x] 0.12.7 bilingual conflict-card route + static render gate: in `ui/sveltekit-app` implement the evidence-first read-only report view (SPEC 21). `src/lib/types.ts` mirrors `Report`/`ReportSummary`/`ConflictCard`/`CardSpan`/`CardCertificate`; `src/routes/+page.ts` imports the build-copied `static/report.json` at prerender; `src/lib/components/ConflictCard.svelte` renders one card in the §21 order — (1) JA source spans (`raw_text` + `table_cell`) first, (2) EN gloss, (3) normalized CKC view (`<pre>` JSON), (4) explanation JA/EN, (5) witness/model, (6) certificate evidence (id+class+solver) and (7) certificate-depth badge, (8) repair candidates, (9) human review question JA/EN, (10) adjudication status; `src/routes/+page.svelte` renders the summary header (counts + depth distribution + taxonomy) then one `ConflictCard` per `conflict_cards` entry, JA-primary/EN-secondary labels. Gate: `cd ui/sveltekit-app && pnpm build && node -e "const h=require('fs').readFileSync('build/index.html','utf8');for(const s of ['敗血症','C7-Kernel','pending_adjudication','norm_contradiction'])if(!h.includes(s))throw new Error('missing '+s)"` exits 0 (the prerendered static HTML embeds the JA source text, depth badge, adjudication status, and conflict type — proving the card renders from the static artifact alone). (103K/200K)
- [ ] 0.13 Replay and determinism
