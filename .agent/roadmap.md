# CKC Roadmap

Phases contain top-level tasks; top-level tasks may contain indented subtasks.
See `.agent/prompt.md` for the session workflow that governs progression.

Top-level task annotations:
- `@<short-hash>`: the Type A commit that opened the task; the start of its
  review range. The first roadmap item uses the initial commit.
- ` reviewed`: a Type R (per-task) or Type P (Phase) review has covered the task.

A trailing `(NNNK/200K)` on a completed subtask records the session's
context-window usage at completion, produced by `python3 .agent/context_window.py`
just before commit. Type A reads the prior top-level task's annotations to
calibrate new subtask granularity.

## Phase 0: Proof-Carrying Research Kernel

- [x] 0.1 Project scaffold @a9a3321 reviewed
- [x] 0.2 CKC schema v0 (SPEC 10) @8f0ae42 reviewed
- [x] 0.3 Normal Form (SPEC 11) @642fabb reviewed
- [x] 0.4 Content-addressed store @c96a48c reviewed
- [x] 0.5 Toy fixtures (SPEC 20 Phase 0 scenarios) @8933fdc reviewed
- [x] 0.6 Terminology and e-graph @f0151f5 reviewed
- [x] 0.7 Retrieval fixture @3a6d8c4 reviewed
- [x] 0.8 Compiler targets (SPEC 14) @24b00a2 reviewed
- [x] 0.9 Verification and certificates (SPEC 13) @aca8d79 reviewed
- [x] 0.10 Conflict detection (SPEC 15) @26da1a2 reviewed
- [x] 0.11 CLI commands (SPEC 18) @8413389 reviewed
- [x] 0.12 Report and UI @60daba3 reviewed
- [x] 0.13 Replay and determinism @7e49863 reviewed
  - [x] 0.13.1 RunManifest JSON Schema + committed run-manifest golden: `RunManifest`/`RunManifestEntry` (`crates/ckc-cli/src/manifest.rs`) already derive `JsonSchema` but carry no committed schema or value golden. Add `crates/ckc-cli/tests/manifest_golden.rs` mirroring `crates/ckc-verify/tests/manifest.rs` (`workspace_root`/`schema_dir`/`golden_dir` helpers, `check_golden`/`check_roundtrip`/`check_schema`, the `golden_suite!` macro, `#[ignore] regenerate` -> `write_type` writing BOTH `schemas/run_manifest.schema.json` and `schemas/golden/run_manifest.json`). Fixture `fn golden_run_manifest() -> RunManifest` binds `let dir = tempfile::tempdir().unwrap();` then returns `ckc_cli::pipeline::run_demo("research-kernel", false, dir.path()).unwrap()` (bind the tempdir to a local so it outlives the write; the fixture uses the pub `run_demo` because integration tests are a separate crate and cannot see `pub(crate)` `run_pipeline`, and `run_demo` returns `run_pipeline`'s manifest unmodified so the golden equals exactly what 0.13.3 `run_replay` re-derives). Run regen once to write both files and commit them. `canonical` asserts a fresh `golden_run_manifest()` byte-equals `schemas/golden/run_manifest.json` (the integrating hash-lock over all 41 entries); `roundtrip`+`schema` lock serde/JSON-Schema stability. `producer_version` embeds `env!("CARGO_PKG_VERSION")` (currently 0.0.0), so a ckc-cli version bump changes the golden bytes -> regenerate via the ignored `regenerate` test (changed input -> changed output, SPEC 4; see the memory producer_version known-issue). Cross-tempdir determinism already lives in `crates/ckc-cli/tests/demo.rs::run_demo_hashes_identically_across_tempdirs` (reference; keep this check single-sourced there). Gate: `cargo test -p ckc-cli --test manifest_golden`; all workspace tests pass. (85K/200K)
  - [x] 0.13.2 ReplayReport model + JSON Schema: new module `crates/ckc-cli/src/replay.rs` defining the structured replay diagnostic (SPEC 18) -- `pub struct ReplayReport { manifest_command:String, status:ckc_core::enums::ReplayStatus, n_entries:usize, n_matched:usize, mismatches:Vec<ReplayMismatch> }` and `pub struct ReplayMismatch { artifact_path:String, expected_hash:Option<ckc_core::canonical::ContentHash>, actual_hash:Option<ckc_core::canonical::ContentHash> }` (derive `Clone,Debug,PartialEq,Serialize,Deserialize,JsonSchema`; reuse `ReplayStatus::{Passed,Failed}` + `ContentHash`; no f64 fields, so serde round-trip is exact, unlike `Report` in 0.12.4). Add `pub mod replay;` to `crates/ckc-cli/src/lib.rs` and `pub use replay::ReplayReport;`. A `#[ignore] regenerate` test writes ONLY `schemas/replay_report.schema.json` via `serde_json::to_string_pretty(&schemars::schema_for!(ReplayReport))` plus a trailing newline (the schema half of the `write_type` idiom, not a call to `write_type` -- 0.13.2 commits no value golden, mirroring how 0.12.1 committed only `report.schema.json`); commit it. `crates/ckc-cli/tests/replay_schema.rs` (mirror `crates/ckc-cli/tests/scaffold.rs`) hand-builds a `Passed` empty-mismatch `ReplayReport` and a `Failed` single-`ReplayMismatch` one (`ContentHash("sha256:..".to_string())` tuple ctor as in scaffold.rs), asserts serde round-trip, and asserts `schemars::schema_for!(ReplayReport)` pretty bytes equal `schemas/replay_report.schema.json`. Gate: `cargo test -p ckc-cli --test replay_schema`; all workspace tests pass. (87K/200K)
  - [x] 0.13.3 compare_manifests + run_replay logic: extend `crates/ckc-cli/src/replay.rs`. `pub fn compare_manifests(expected:&RunManifest, actual:&RunManifest) -> ReplayReport` (PURE): index both entry lists by `artifact_path` (unique across the 41 entries -- each stage emits distinct paths -- so indexing needs no dedup); emit a `ReplayMismatch` for every path whose `content_hash` differs or that is present on only one side (`expected_hash`/`actual_hash` `None` for the absent side), sorted by `artifact_path`; `n_entries=expected.entries.len()`, `n_matched` = count of expected entries whose path appears in actual with identical `content_hash`, `status = if mismatches.is_empty() { Passed } else { Failed }`. `pub fn run_replay(manifest_path:&Path, out_dir:&Path) -> anyhow::Result<ReplayReport>`: read + `serde_json::from_slice` the expected `RunManifest`; `bail!` unless `expected.command == pipeline::DEMO_COMMAND` (Phase-0 replay is single-scenario -- the command-equality gate stands in for scenario selection because research-kernel is the only scenario per `run_pipeline`'s assert; command-parsed selection is deferred); re-derive `let actual = pipeline::run_pipeline("research-kernel", out_dir)?`; `let report = compare_manifests(&expected, &actual)`; `emit::write_artifact(out_dir, "replay_report.json", &to_canonical_bytes(&report))?` (persist the diagnostic BEFORE any bail); `bail!` with the mismatch list when `report.status != ReplayStatus::Passed`, else `Ok(report)`. Promote `run_pipeline` and `DEMO_COMMAND` in `pipeline.rs` to `pub(crate)` (both currently private; same-crate `run_replay` consumes them, and `run_pipeline` writes no `run_manifest.json` so `run_replay` writes only `replay_report.json`). `crates/ckc-cli/tests/replay.rs` (with a `workspace_root` helper as in demo.rs/report.rs): (a) positive -- `run_replay(&workspace_root().join("schemas/golden/run_manifest.json"), tmp.path())` is `Ok`, `status==Passed`, `n_matched==n_entries==41`, `mismatches` empty, `tmp/replay_report.json` exists; (b) negative -- `fs::read`+`from_slice` the committed golden as `committed`, clone it and flip one entry's `content_hash` into `tampered`, assert `compare_manifests(&committed, &tampered).status==Failed` with exactly one mismatch at that `artifact_path` carrying `expected_hash!=actual_hash`; (c) determinism -- two `run_replay` calls into separate tempdirs both `Ok(Passed)`. Gate: `cargo test -p ckc-cli --test replay`; all workspace tests pass. (116K/200K)
  - [x] 0.13.4 ckc replay command + binary acceptance: add `Command::Replay { manifest:PathBuf, #[arg(long, default_value = DEFAULT_OUT_DIR)] out:PathBuf }` to the `Command` enum in `crates/ckc-cli/src/main.rs` (doc `/// Replay a committed run manifest and compare hashes (SPEC 18)`), add `replay` to the `use ckc_cli::{...}` import line, and dispatch `Command::Replay { manifest, out } => { replay::run_replay(&manifest, &out)?; }` (the `?` propagates `run_replay`'s mismatch `bail!` to a non-zero exit). `crates/ckc-cli/tests/replay_cli.rs`: (1) spawn `env!("CARGO_BIN_EXE_ckc")` with `["replay", <abs schemas/golden/run_manifest.json>, "--out", <tmp>]`, assert `status.success()` and `tmp/replay_report.json` exists and deserializes with `status==Passed`; (2) bail-path coverage (genuinely new -- demo.rs already covers `demo --replay` exit 0): write a TAMPERED manifest into a tempdir (clone the committed golden, flip one entry's `content_hash`, `to_canonical_bytes`, keeping `command==DEMO_COMMAND` so the command gate passes), spawn `["replay", <tampered-path>, "--out", <tmp2>]`, assert NON-zero exit AND `tmp2/replay_report.json` exists and deserializes with `status==Failed` (run_replay persists the diagnostic then bails end-to-end through the binary). Gate: `cargo test -p ckc-cli --test replay_cli`; `scripts/ci.sh` green; all workspace tests pass. (81K/200K)
