# markdown-lsp

Markdown LSP via `marksman`.

Install: download the latest release binary from
`https://github.com/artempyanykh/marksman/releases/latest` to `~/.local/bin/marksman`
and `chmod +x` it.
